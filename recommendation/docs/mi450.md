# Bring-up on MI450 A0 (gfx1250, single GPU)

Running the yambda-5b HSTU ranker on an **MI450 A0 engineering sample**. The
stack works — fbgemm builds, TorchRec shards, the 4.76B-event dataset streams,
the benchmark reaches `run_stop` — but **training does not survive**: the loss
goes `nan`. Best known configuration trains correctly for ~2150 steps (~12 min),
loss decreasing and matching the PyTorch reference, then poisons:

```bash
IMG=recommendation-amdprimus0815:triton-3.7.1-amd   # Triton 3.7.1, AMD build
AMDGCN_USE_BUFFER_OPS=0                             # default in the runner
./scripts/run_smoke_shrink_1gpu.sh
```

### Images tested

All four repo images are the same [`Dockerfile.amdprimus0815`](../Dockerfile.amdprimus0815)
build — same torch `2.11.0+rocm7.14.0a20260625`, same ROCm, same fbgemm and
torchrec — with **only the Triton wheel swapped** (`--no-deps`). So Triton is the
only variable across them. Select one with `IMG=`; the runner defaults to the
3.6.0 image.

- **`recommendation-amdprimus0815:gfx1250`** — Triton `3.6.0+rocm7.14.0` (AMD SDK,
  as shipped in the base image). Runner default. `nan` by step 50.
- **`recommendation-amdprimus0815:triton-3.7.1-amd`** — Triton
  `3.7.1+git0263a6a6.rocm7.14.0`. **Best known**: trains to step 2150.
- **`recommendation-amdprimus0815:triton-3.8.0-amd`** — Triton
  `3.8.0+git4cff872c.rocm7.14.0`. Memory access fault at `gstep=0`. One root
  cause is isolated and fixed (row 4) but at least one more remains, so this
  image is still unusable.
- **`recommendation-amdprimus0815:triton-main`** — Triton built from `main` @
  `58895270`. Will not compile the kernels. (Built via
  [`scripts/build_triton_from_source.sh`](../scripts/build_triton_from_source.sh);
  note `docker commit` of a `sleep`-kept container inherits that `ENTRYPOINT`, so
  this tag resets it back to `bash`.)

Base images, for reference: `amdprimus/amdprimus:0815` is what the repo image is
built `FROM`, and `amdprimus/amdprimus:gfx1250-20260831` was evaluated and
rejected — its `libtriton.so` is byte-identical to `:0815`, so a newer image is
not a newer Triton.

Branch `chcai/mi450_a0`. Companion to [`training_recipe.md`](./training_recipe.md)
and [`perf_opt.md`](./perf_opt.md).

## Everything known about the open problems

All are in the Triton path; none is in the benchmark's math. Rows 1 and 2 are
ready to hand to the Triton team as-is; row 3 is the blocker. Only the Triton
wheel was ever swapped (`--no-deps`), holding torch, ROCm and driver constant.

| # | Problem, symptom and trigger | Affected Triton versions | Mitigation | Reproducer |
|---|---|---|---|---|
| **1** | **GPU hang, node unrecoverable.** `HW Exception by GPU node-2 … reason :GPU Hang`, 100 % GFX / 0 % HBM. Trigger: a kernel whose pointer args straddle the **2 GiB narrowing cutoff**, so one body carries both 32-bit `buffer_*` and 64-bit `global_*` addressing. `HIPBackend.get_tensor_specialization` sets `tt.pointer_range=32` when `use_buffer_ops and arg.untyped_storage().size() <= 2**31 - 1`; in the failing kernel `JaggedIn` (4.064 GiB) and `OutA` (4.062 GiB) stay 64-bit while `OutB` and the offsets narrow. Confirmed hybrid in asm: 16 `buffer_store_b128` + 8 `global_load_b128` + 8 `global_store_b128`. **Not** an infinite loop — every config compiles, one `s_endpgm` each, Tarjan SCC over the CFG finds no cycle. Masking is by address not predicate: masked lanes get sentinel offset `0x80000000`, expected to be dropped for exceeding `num_records=0xffffff`. Crossing 2 GiB is **necessary but not sufficient** — a *passing* dense bf16 case is also above the cutoff but takes different `IS_DENSE_*` branches. Real workload hits it: at batch 128 `concat_2D_jagged` crosses 12× (largest arg 2135 MiB, 1.04×) and hangs; at batch 32 the largest is 552 MiB (~3.7× under), so small runs never see it | **every** version tried: `3.6.0+rocm7.14.0` (AMD SDK, in `amdprimus:0815`), `3.8.0` (PyPI `v3.8.0`), `3.8.0` @ `main` `76940ad3` (source; **regression** — also hangs `position_test::…_large_tensor`, which passes on 3.6.0). `3.7.1+git0263a6a6` and `3.8.0+git4cff872c` (both AMD `repo.amd.com/rocm/whl-multi-arch/`) emit byte-identical hybrid codegen so are **assumed** affected; launch not run. `amdprimus:gfx1250-20260831` has a byte-identical `libtriton.so` to `:0815` — a newer image is not a newer Triton | **`AMDGCN_USE_BUFFER_OPS=0`**. Disables `canonicalize_pointers` → `convert_to_buffer_ops` → `optimize_buffer_op_ptr` together, so the A/B implicates the **group**, not one transform. `AMDGCN_ANALYZE_SMALL_TENSOR_RANGE=1` is the other knob on the group; untested | Unit test, **backward** pass, inside `do_bench` autotuning: `jagged_tensors_test.py::JaggedTensorsTest::test_concat_2D_jagged_large_tensor` → `split_2D_jagged_multirow` @ `BLOCK_N=2, num_warps=1`. Also `position_test.py::…_positional_embeddings_triton_large_tensor` → `_add_embeddings_bwd_kernel` @ `BLOCK=128, num_warps=8, num_stages=4` (on `main` only). Or, needing **only torch+triton**, [`scripts/repro_gfx1250_buffer_ops.py`](../scripts/repro_gfx1250_buffer_ops.py) — verbatim kernel, autotuner removed, config pinned, so it is a single deterministic dispatch: `--compile-only` is safe; `AMDGCN_USE_BUFFER_OPS=1` hangs, `=0` passes. **Run one test per process with a hard timeout** — `pytest` orders `unittest` methods alphabetically, so a sweep blames the wrong test. Expect a **reboot** per hang |
| **2** | **Silent wrong results, kernel completes.** 20 / 7640 elements wrong, max abs diff 1.38 against tol 1e-5. A few badly-wrong lanes is the signature of a mis-lowered mask or bounds check, consistent with the address-sentinel masking in row 1 | same as row 1 | same knob | Unit test `position_test.py::test_add_timestamp_positional_embeddings_triton`. Seconds, does not wedge |
| **3** | **`train_loss=nan`, and it stays.** Reaches the loss via **poisoned embedding tables** — at the first non-finite forward output, 80 params are already non-finite, all embedding tables, so a Triton *backward* emits non-finite gradients that the TBE update writes in and every later forward inherits. **Not divergence:** `lr≈4e-9` cannot diverge, but one NaN gradient poisons a weight permanently since `NaN × 4e-9` is still `NaN`. Init is clean (shared with the passing PyTorch run). That it merely *moves* from step 50 to step 2200 across compiler versions points at a **nondeterministic corruption** — under-masked store or race — not a fixed miscompile: codegen changes how often it is hit, not whether | `3.6.0`: `nan` by **step 50**. `3.7.1+git0263a6a6` (AMD): finite and tracking PyTorch to step 2150, `nan` from **step 2200** — a ~44× delay, **not a fix**. `3.8.0+git4cff872c` (AMD): cannot be assessed, dies first (row 4). `main` @ `58895270`: cannot be assessed, will not compile (row 5). **A 250-step run is not enough to clear a version** — 3.7.1 looked fixed at 250 steps; only the 30-min run exposed it. Future claims need **≥3000 steps** | None. Triton 3.7.1 buys ~12 min. `HSTU_HAMMER_KERNEL=PYTORCH` avoids it entirely — finite and stable, 0.13847→0.14034 over 250 steps — but is slower, and is what proves the defect is in a Triton kernel rather than the model, data or optimizer | **e2e only; no unit test reproduces it.** 60 steps (~4 min) on 3.6.0; **≥2200 steps (~12 min)** on 3.7.1. Check `ps -eo pid,cmd \| grep train_ranker` first — an interrupted run can outlive its shell, and two trainings sharing the GPU invalidates the result. `DEBUG_NAN_HOOKS=1` names the first module emitting a non-finite value and flags the kernel that *manufactured* a bad gradient (finite `grad_output` in, non-finite `grad_input` out, logged `PRODUCER`) versus those propagating it — meaningful only on the **first** poisoned backward. `=2` also sweeps all params pre-forward, opt-in because that sweep has itself wedged the GPU (row 1b) |
| **1b** | **Hangs that survive the knob.** Same `GPU Hang` signature with buffer ops **off**, so the mitigation clears the two *known* hangs, not the class. Seen on an fp16 position test, and on a plain read of the embedding tables | `3.6.0` | none known | Not isolated; still owed a matched control |
| **4** | **`3.8.0` computes wrong addresses in HSTU attention.** `Memory access fault by GPU node-2 … on address 0x2000. Reason: Page not present or supervisor privilege` at `gstep=0`, before step 1; the address **varies run to run** (`0x2000`, `0x3000`) and is a near-null base plus a block offset, not a fixed bad pointer. Attributed with `AMD_SERIALIZE_KERNEL=3`, which makes the async fault synchronous: `_weighted_layer_norm_fwd` autotuning *completes* (3.36 s, best `BLOCK_N=4, num_warps=2`), then the **attention** kernel is traced, then the fault on its first launch. The unit test below shows the same defect landing on *mapped* memory instead: 485/1280 elements wrong (37.9%) with greatest absolute difference **3.3e+28** — the magnitude of garbage, not of a precision error, so the kernel is reading from wrong addresses either way. **Root cause: `3.8.0` miscompiles the *software-pipelined* attention loop. With `num_stages >= 2` it faults or returns garbage; with `num_stages == 1` it is bit-comparable to 3.6.0.** The forward is **pinned to a single config** on HIP (`BLOCK_M=128, BLOCK_N=32, matrix_instr_nonkdim=16, kpack=2, num_stages=2, num_warps=8`), so there is no autotuning involved and exactly one config faults. Sweeping it: every failing combination carries `num_stages=2` (`m=32/64/128`, `warps=4/8` all fail with it), every `num_stages=1` combination passes, and dropping the AMD-specific `matrix_instr_nonkdim`/`kpack` knobs changes nothing — they are **not** implicated. Decisively, with `num_stages=1` and the **original block pointers left in place**, all 8 shapes pass on 3.8.0 at 3.6.0-identical accuracy (6e-08 forward, 1e-06 gradients). **This retracts the earlier "transposed block pointer" root cause**, which was a symptom of the same bug: crossing `num_stages` against the K operand source in [`scripts/repro_gfx1250_pipeliner.py`](../scripts/repro_gfx1250_pipeliner.py) shows `num_stages=1` exact for *both* block-pointer and plain-pointer K, while `2` and `3` are wrong **only** for the transposed block pointer, with 3.6.0 clean in all six cells. The earlier probe simply pipelined by default. The transposed operand is therefore the *minimal trigger*, useful for a bug report, but the real loop breaks under pipelining regardless of pointer style — with Q/K/V all on plain arithmetic and `num_stages=2`, the kernel still faults. `tl.advance` was ruled out earlier and stays ruled out. Kept for the record: the load in isolation is exact on every version, so plain addressing was never the issue. **Recoverable** — unlike the hangs the GPU came back clean with no reboot, which also makes it a far better bug report than a wedge | `3.8.0+git4cff872c` (AMD), with buffer ops **off** | **Applied globally: `clamp_num_stages` in `common.py` forces `num_stages=1`** on every autotuned kernel when on HIP with Triton ≥ 3.8. It sits inside the OSS `triton_autotune` wrapper, which all eight `ops/triton` kernel files route through, so one edit covers them all; configs are mutated in place so per-config extras survive — some backward configs carry a required `pre_hook` that rebuilding would drop. `TRITON_ALLOW_PIPELINING=1` keeps the original staging (to re-check a newer build), `=0` forces the clamp on a version that does not get it by default. **Inert on 3.6.0 and 3.7.1**, which still report `num_stages=2` and pass. **This clears attention completely but does not make 3.8.0 usable.** Scoping it to attention alone moved the e2e fault from `gstep=0` deep into the *backward* pass (past `_weighted_layer_norm_bwd_dx`, faulting after `concat_2D_jagged_multirow` with `HSA_STATUS_ERROR_MEMORY_APERTURE_VIOLATION`) — evidence the defect is general, since those kernels autotune to `num_stages=3`. Clamping globally then removes the fault at that point but the run appears to **hang** instead: 100 % GFX, **0 % HBM**, 855 W, no step ever logged. **That "hang" is the same fault, not a hang** — re-running under `AMD_SERIALIZE_KERNEL=3` turns it back into `HSA_STATUS_ERROR_MEMORY_APERTURE_VIOLATION`, so without serialization the queue simply wedges behind the async fault. It is **not** pipelining: the kernels around it now autotune to `num_stages: 1`, confirming the clamp is applied. Attributed by filtering `AMD_LOG_LEVEL=3` to `ShaderName`: the last dispatch before the violation is **`_ln_mul_dropout_bwd_dx_du`** — a **separate defect, now root-caused and worked around in row 4c**. With both workarounds in place **3.8.0 completes a 20-batch smoke run**, the first time it has finished one. Unlike row 1 the fault was **killable and the GPU recovered without a reboot** (verified by re-running the attention harness clean afterwards). Perf cost of losing pipelining everywhere is unmeasured and likely material, so keep using 3.7.1 until it is | **Preferred, and what the bisection above was done with:** [`scripts/attn_check.py`](../scripts/attn_check.py) — fixed shapes instead of hypothesis, comparison on CPU copies, and forward/backward reported separately, so it takes **10 s instead of 143 s**, is deterministic, and survives the `assert_close` abort that otherwise hides the numbers. `--fw-config m=128,n=32,stages=1,warps=8` overrides the pinned config (it patches `Autotuner.configs`, no repo edit), `--case` picks one shape, `--skip-backward` narrows further. The stock unit test still works — `hstu_attention_test.py::HSTUAttentionTest::test_attn_triton`, which needs `import fbgemm_gpu` before pytest and `pip install hypothesis`, and note `test_attn` in that file is a helper pytest mis-collects, so name the class test explicitly — but it is slower and its shapes move run to run. Or e2e, which faults within ~5 min. **For a Triton bug report:** [`scripts/repro_gfx1250_pipeliner.py`](../scripts/repro_gfx1250_pipeliner.py), which needs neither the repo nor a dataset, runs in seconds, and prints `DEFECT PRESENT` / `DEFECT ABSENT` with its own controls so it is meaningful on a healthy version too. A fault writes a coredump the size of allocated VRAM (9–36 GB); `HSA_ENABLE_COREDUMP=0` does **not** suppress it on this runtime, so the runner also sets `HSA_COREDUMP_PATTERN` to keep it out of the working tree |
| **4b** | **`3.7.1` attention is marginally out of tolerance — the version we actually train on.** Found while controlling for row 4: the attention unit test also fails on `3.7.1`, at clean `HEAD` with no local edits, so it is pre-existing. But the failure is **precision-scale, not corruption** — greatest absolute difference **1.38e-05 against a 1e-05 tolerance** (12/63104 elements) on one hypothesis case and 2.40e-05 (1108/259584, 0.4%) on another. The reported `inf` relative difference is only a zero reference. Contrast 3.8.0's 3.3e+28 garbage and 3.6.0's clean pass: 3.7.1 shifts instruction selection just enough to drift a few elements past a tight tolerance. **Open question whether this is benign fp drift or the leading edge of row 3** — it is the only known numeric discrepancy on the version we train on, which makes it the natural next thread to pull for the `nan` | `3.7.1+git0263a6a6` (AMD) | none; magnitudes are at tolerance scale, and the e2e run tracks PyTorch to step 2150 | Same unit test as row 4, run on 3.7.1 (~94 s) |
| **4c** | **`3.8.0` miscompiles the fused-RNG layer-norm-mul-dropout backward, but only with dropout on.** `HSA_STATUS_ERROR_MEMORY_APERTURE_VIOLATION: The agent attempted to access memory beyond the largest legal address`, with the runtime naming `kernel: _ln_mul_dropout_bwd_dx_du` outright. What is left of 3.8.0's e2e failure once row 4's pipelining clamp is in. **Found by dumping the real launch arguments rather than guessing shapes** — a shape sweep had already cleared this kernel because it used `dropout_ratio=0.0`. Wrapping the launch to record every argument and replaying the last one gives an **8-second standalone reproducer**: `N=18187, D=512` bf16, `CONCAT_U=CONCAT_X=COMPUTE_Y=True`, `SILU_U=False`, `TRAINING=True`, `dropout_ratio=0.1`, `num_warps=2`, grid `(4546,)` = `N//4`, and it faults on **call #1**. **Dropout is the whole discriminator:** identical launch with `dropout_ratio=0.0` passes, and 3.6.0 passes either way. Forward is fine; only the backward faults. **Not the row 4 pipelining defect** — this kernel is a plain `@triton.jit` launch that passes no `num_stages`, so the autotuner clamp never sees it, and forcing `num_stages=1`, `2` or `3` faults identically. `FAST_DROPOUT=True` (`rand3x` instead of three `tl.rand`) does **not** avoid it either. The dropout code only calls `tl.rand` and `tl.where`, which touch no memory, so the fault means **addressing is being clobbered**, consistent with register allocation at `BLOCK_D=512`; a stripped kernel with the same grid-strided loop and the same three `tl.rand` calls is **clean**, so it needs the full kernel's pressure and does not reduce further | `3.8.0+git4cff872c` (AMD), with buffer ops **off** and the row 4 clamp applied | **Force the separated-RNG path on gfx1250 for Triton ≥ 3.8** (`_fused_rng_ln_mul_dropout_is_broken` in `ops/utils.py`, folded into `use_separated_rng_ln_mul_dropout`). That precomputes the dropout mask and dispatches `_ln_mul_dropout_bwd_dx_du_rng` instead, avoiding the faulting kernel entirely. It is an **upstream-supported path**, already taken by sm100-103 and MI350 (gfx950); gfx1250 simply did not match the gate. Gated on Triton ≥ 3.8 so **3.6.0 and 3.7.1 are untouched** — whether gfx1250 wants this path on its own merits is a *performance* question, not to be settled on a miscompiling version. **With this plus row 4's clamp, 3.8.0 finished a 20-batch smoke run with no fault, no hang and no `nan`** (NE 1.4817, and it reached `run_stop` and eval) — the first time it has completed a run. **But 20 batches does not generalise — a longer run still faults, a few hundred batches in; that is row 4d.** Separately, `DEBUG_NAN_HOOKS=1` is **not usable on 3.8.0** to chase it: with hooks installed the run faults at `gstep=0` in the first backward, consistent with row 1b's "plain read of the embedding tables" | Standalone in ~8 s: `triton_norm_mul_dropout` fwd+bwd at the recorded arguments above, comparing `dropout_ratio=0.1` against `0.0` and 3.8.0 against 3.6.0. Needs the repo but no dataset. To re-find an argument set like this, wrap the kernel object with a recorder that dumps shapes, strides, dtypes and constexprs and `fsync`s on every call, overwriting — the fault is asynchronous and kills the process without unwinding, so the last file written is the faulting launch |
| **4d** | **`3.8.0` faults *nondeterministically* in the HSTU attention backward, a few hundred batches in.** What is left of 3.8.0 once rows 4 and 4c are worked around. `Memory access fault by GPU node-2 … Page not present or supervisor privilege`, and under `AMD_SERIALIZE_KERNEL=3` the runtime names it: **`kernel: _hstu_attn_bwd`**. **The batch index moves between runs — 216 unserialised, 351 serialised** — and a 250-batch run completed all 250 cleanly, which is consistent with the fault simply lying beyond 250 in that run. All runs read the **identical** data window (`[12960040, 13046440)`, 13,940,552 anchors, `global_batch_size=8`, verified in the logs), so batch count is the only variable and the moving index is **not** a data-dependent trigger at a fixed batch. **Row 4's clamp does not cover it**: the backward *is* autotuned, so it already runs at `num_stages=1`, and the forward is clean. **This is the most interesting result on the board**, because `_hstu_attn_bwd` is the *same kernel* as row 4b's 3.7.1 `dk`/`dv` drift and the natural suspect for row 3's `nan` — the attention backward misbehaving nondeterministically on two compiler versions, as a **hard fault** on 3.8.0 and as **bad gradients** on 3.7.1, is one candidate defect rather than three. Cheap to hit, so it is a far better handle than row 3's 2200-step wait. **Narrowed but not yet reproduced standalone.** Three facts to build on. (i) **The config is not a variable:** the backward is *pinned* on HIP to one config (`BLOCK_M=32, BLOCK_N=128, matrix_instr_nonkdim=16, waves_per_eu=0, SEQUENCE_PARALLEL=False, UNROLL=1, num_stages=1, num_warps=4`), so `SEQUENCE_PARALLEL` is off and the `LOCK` spin-lock path is **not** in play, and `num_stages` is already 1 so row 4's clamp is inert here. Every config does carry the required `pre_hook` that zeroes `DQ` (and the `LOCK`, which is otherwise `torch.empty`), so uninitialised-accumulator is ruled out. (ii) **The faulting batch is not an outlier.** Recording all 408 launches of a run that died at batch 135 shows the faulting batch is *small* — 9429 rows against a 17672 mean (7th percentile), `MAX_SEQ_LEN=1405` against a typical ~2800, lengths `[499, 500, 1405×6]`, `num_targets` all 1, and the same `Q` stride as every other launch. The three launches of that batch (one per HSTU layer) are identical, so the shape is unambiguous even unserialised. (iii) **Shape and layout alone do not reproduce it:** 200 iterations of exactly that batch pass on 3.8.0, both with contiguous `q/k/v` and with the true fused-`uvqk` views (row stride **2048**, not the contiguous 512, verified against the recording). So the trigger needs something the isolated kernel does not have — allocator/memory state, concurrent activity, or corruption from an earlier kernel — which is also why the moving batch index is better explained by run-to-run *data order* (multiple dataloader workers) than by the shape itself | `3.8.0+git4cff872c` (AMD), buffer ops **off**, with the row 4 clamp and row 4c workaround applied | none yet | e2e, ~200–400 batches (~7–10 min unserialised; serialised is *faster* per batch here and reaches 351 in ~4 min). **Set `PROGRESS_EVERY=1`** — the `[lr-warmup]` line fires only once, so without it a run that dies mid-window looks identical to one that never started, which is what made this look like an immediate `gstep=0` fault at first. Serialisation is what names the kernel; without it you only get an address. `attn_check.py` does **not** reproduce it — all 8 shapes pass, so it needs either the e2e shapes or many launches |
| **5** | **Upstream `main` will not compile this codebase.** `Block pointers have been removed in favor of the tensor descriptor API` — the HSTU kernels call `tl.make_block_ptr`. An upstream API removal, not a gfx1250 bug | `main` @ `58895270` (built with sglang's recipe, [`scripts/build_triton_from_source.sh`](../scripts/build_triton_from_source.sh), `pip install -e .`; pass `TRITON_COMMIT`) | Port the kernels to `TensorDescriptor`, or stay on 3.7.1 | Compile fails at model construction; no GPU needed |

Questions for the Triton team: (1) Is a hybrid kernel — some pointers narrowed to
32-bit buffer ops, others left 64-bit, in one body — intended and tested on
gfx1250, or does the pass group assume all-or-nothing narrowing? (2) Is
`0x80000000`-offset-plus-`num_records` masking sound on RDNA wave32 given
`num_records = 0xffffff`? (3) Do
[#11313](https://github.com/triton-lang/triton/pull/11313),
[#11246](https://github.com/triton-lang/triton/pull/11246),
[#11379](https://github.com/triton-lang/triton/pull/11379),
[#10804](https://github.com/triton-lang/triton/pull/10804) bear on this? We
tested `main` @ `76940ad3` and still reproduce. (4) Is gfx1250 covered by CI for
the buffer-op path above 2 GiB? (5) Separately, for row 4: which `3.8.0` change
altered the dot-operand layout for a block-pointer load with `order=(0, 1)`, and
is that path covered by CI on gfx1250? It is exact on `3.6.0` and `3.7.1`, the
load is exact in isolation on `3.8.0`, and only the transposed operand of
`tl.dot` regresses.

## The node

1× MI450 A0 engineering sample, `gfx1250`, PCI `1002:75c1`, SKU `M4500001`, rev
`0x00` — 256 WGPs, ~432 GiB HBM, **`warp_size=32`** (RDNA, not CDNA wave64).
ROCm 7.14, torch `2.11.0+rocm7.14.0a20260625`, Triton 3.6.0 stock. Host Linux
`6.14.0-37-generic`, amdgpu DKMS 7.1.1, Docker 29.6.1.

Two facts drive every change here: **one GPU** (the whole model must fit in one
HBM) and **wave32** (CDNA assumptions in fbgemm are wrong).

`amdgpu` is blacklisted by the kernel cmdline
(`modprobe.blacklist=amdgpu,device_dax,dax_hmem`), so after every boot run
`sudo modprobe amdgpu` — `/dev/kfd` appears ~15 s later.

**Recovery from a hang is a reboot.** The process sticks in uninterruptible `D`
state, `docker kill` returns *"did not receive an exit event"*, MODE2 reset fails
with `error code -62`, and `modprobe -r amdgpu` deadlocks. The wedge is
whole-device and survives container teardown: `rocm-smi` still answers while
holding VRAM with no processes alive, and a bare 1024² bf16 matmul in a fresh
container never returns. Untried: `flr`/`bus` reset and the `umr` wave debugger.

## Getting it to run

**Image** — [`Dockerfile.amdprimus0815`](../Dockerfile.amdprimus0815). Stock
`rocm/primus:v26.3` torch has no gfx1250 code objects, so *any* kernel fails with
`CUDA error: device kernel image is invalid`. `amdprimus/amdprimus:0815` has
gfx1250 torch but no fbgemm/torchrec, so those are built on top (~8 min). ROCm
lives in the wheel at `site-packages/_rocm_sdk_devel`, not `/opt/rocm`, so
`ROCM_HOME`/`HIP_ROOT_DIR` must point there or configure dies with
`BUILD_ROCM_VERSION is not set`. Also needs `libtbb-dev`, `libnuma-dev`,
`libssl-dev`, `ninja-build`.

**Three fbgemm patches** — (1) `Fbgemm.cmake` omits `FbgemmFloat16Convert*.cc` /
`FbgemmBfloat16Convert*.cc`, relying on torch's bundled FBGEMM; (2) host/device
**warp-size disagreement**, host assumed wave64; (3) codegen also hardcodes
wave64. With these, TBE is **bit-exact** against CPU.

**Embedding shrink** — `EMBEDDING_ROW_SCALE`; the reference tables are ~559 GiB
of fp32 weights against one ~432 GiB GPU.

**Dataset** — use the **MLCommons prepared download** (README Option A): 227.41
GB, 19 files, all md5/sha256 verified. Do **not** self-preprocess; the cache build
needs ~190 GB RAM and OOM-killed this 251 GB box.

## Performance

Triton 3.7.1, gin-default depth 3, `MAX_SEQ_LEN=4096`, `EMBEDDING_ROW_SCALE=0.25`,
`BATCH_SIZE=128`, buffer ops off — measured in the finite region, while the loss
was still converging. Step 50 is warmup (1920 ms, carrying compile and autotune).
Steady state is **~322 ms/step, ~395 samples/s, `tflops_real` 35–61, HFU
1.1–1.9%**. HFU tracks `fill` (29–49%) rather than being a fixed number, because
`fill` is the fraction of the dense-equivalent work the jagged batch actually
contains.

**Read `hfu`, not `mfu`.** `num_flops_per_sample` is a *static dense* estimate
(315.7 GFLOP/sample at 3 layers) while jagged attention performs only the `fill`
fraction of it, so `mfu` (3.7–4.2%) describes work that is never executed.
`tflops_real` is the honest count and `hfu` its utilization.

HFU is against **3150 TFLOP/s = 90% of the 3500 TFLOPS dense BF16 matrix peak**
for MI450. Two notes on that denominator. `get_gpu_peak_flops` matched nothing and
silently fell back to **MI350X's 2300 TFLOPS**, because these parts report the
generic name `AMD Radeon Graphics`, so every earlier perf line in this repo's logs
is normalized against the wrong chip; it is now keyed on `gcnArchName`. And the
silicon peak is used deliberately, not the ~75 TFLOP/s (~2% of it) this node can
actually reach today: measured bf16 GEMM is only **1.46× fp32** (67.8 vs 46.5 TF
at N=8192), i.e. the **matrix cores are idle** and everything runs on the vector
path at ~1.1 GHz against a 2.4 GHz max clock. Normalizing against measured
throughput would conflate model efficiency with BLAS immaturity and would
silently change meaning as kernels improve.

## Retracted

`and`/`or` on tensors is **not** a bug, and the `triton_position.py` rewrite was
cosmetic. Triton warns `Logical operators 'and' and 'or' are deprecated for
non-scalar tensors`, but `code_generator.py:visit_BoolOp` (3.6.0) lowers them to
`logical_and`/`logical_or`, which for boolean masks is exactly `&`/`|`. Nothing
goes unmasked. The position test passing afterwards was nondeterminism.

Separately, `hstu_compute::test_compute_output` was an **inherited code bug, not a
GPU bug**: the forward dropped `mul_u_activation_type`, so it applied no
activation while its own backward applied one. A one-line fix takes that parameter
grid from 32 mismatches to 0. Our file is byte-identical to
`mlcommons/training@master`, and the defect is dormant in the benchmark, which
only ever passes `"none"`.

## Change inventory

- `Dockerfile.amdprimus0815` — gfx1250 image: gfx1250 torch plus fbgemm built from
  source with the three wave32 patches.
- `scripts/run_smoke_shrink_1gpu.sh` — defaults `AMDGCN_USE_BUFFER_OPS=0` and
  `HSA_ENABLE_COREDUMP=0`; forwards `DEBUG_NAN_HOOKS`; `IMG` selects the image.
- `scripts/repro_gfx1250_buffer_ops.py` — standalone hang reproducer, torch+triton
  only.
- `scripts/repro_gfx1250_pipeliner.py` — standalone reproducer for row 4's
  transposed block-pointer `tl.dot` miscompile, torch+triton only, seconds to run.
- `scripts/build_triton_from_source.sh` — builds Triton at any `TRITON_COMMIT`,
  same recipe as sglang's Dockerfile.
- `dlrm_v4/utils.py` — peak-FLOPS table keyed on `gcnArchName` for gfx1250, so HFU
  stops using MI350X's number.
- `dlrm_v4/train/train_ranker.py` — `DEBUG_NAN_HOOKS` non-finite forward/backward
  hooks (diagnostic for row 3).
- `ops/triton/triton_hstu_linear.py` — upstream bug fix: forward now passes
  `mul_u_activation_type`.
- `common.py` — `clamp_num_stages` forces `num_stages=1` on every autotuned kernel
  on HIP + Triton ≥ 3.8 (row 4); inert on 3.6.0/3.7.1, forced either way with
  `TRITON_ALLOW_PIPELINING`.
- `scripts/attn_check.py` — deterministic attention check (fixed shapes, CPU-side
  comparison, forward and backward reported separately, `--fw-config` to override
  the pinned autotune config). 10 s; this is what the row 4 bisection used.
- `ops/triton/triton_position.py` — cosmetic: `&` instead of `and`, silencing
  Triton's deprecation warning.
