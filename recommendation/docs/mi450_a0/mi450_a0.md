# MI450 A0: NaN investigation and bring-up history

Updated **2026-09-21 UTC**. This is the maintained A0 record, including the
historical changes from the MLPerf reference, their costs, and conditions for
removal. The [porting and rollback record](#historical-porting-changes-and-rollback-record)
restores that information from `8377ae9`; the [complete historical edition](history_8377ae9.md)
retains the original evidence, defect tables, commands, and file inventory.

## Current NaN status

<a id="current-status"></a>

**Investigation stopped at the user's request on 2026-09-21.** The final
08:33 UTC shutdown check found no GPU-device owners across 3,390 process
and thread FD tables, no experiment process under the session paths, and
no trace instances or installed kprobes. All seven completed runtime workers
are reaped with exit code 0; all three investigation agents are interrupted.
The original boot and loaded driver remain unchanged. The repeated zero-DY
comparison is **unrun, with preparation incomplete**; the SVM/queue tracing
probe is **CPU-reviewed but unrun**, with no probe tracing activated. No
experiment or package completion is pending. No commit or push was made.
[Final shutdown record and artifact inventory](evidence/current_20260919/session_stop_20260921.json).

**Whole-kernel sink relocation now prevents the complete controlled
attempt-405 corruption.** All 16 calls pass the frozen full-output matrix:
the trapped control reproduces every historical DW byte in four calls,
while the control without traps and both relocated arms return every
ordinary baseline byte. Both trapped images retain the seven selected
bank05/bank46 events at visit9415; the candidate moves live values out of
physical v1/v257/v513 throughout the kernel. All native argument, owned-ELF,
complete-input, library and terminal audits pass. This validates the
controlled intervention beyond changing only the trigger-site bank modes;
natural failure suppression and training repair remain unvalidated.
[Sixteen-call matrix](evidence/current_20260919/DW_sink_quartet_matrix.json),
[root result](evidence/current_20260919/DW_sink_quartet_result.json).

The original driver and boot remain unchanged. Post-run logs add an SVM
restore-work CPU-hog warning and a corrected PCIe-link-class machine check,
with the same signature already present before this session. The MCE was
reported after the four arm controllers exited; reporting time does not
establish when the underlying error occurred. Neither the exact endpoint
nor a connection to the NaN mechanism is established. These messages remain
in the runtime evidence; the quartet's kernel log is not byte-unchanged.
[Independent MCE/SVM review](evidence/current_20260919/runtime_corrected_MCE_SVM_review.json).

**The complete register-relocation candidate now preserves the saved-input
arithmetic in all eight matched calls.** Four control and four candidate
outputs equal the entire ordinary baseline: 131,072 BF16 words per call,
zero differing bytes. Both native/input/library/lifecycle audits pass.
The candidate moves live data away from physical v1/v257/v513 throughout
the kernel; both arms retain the same added bank-switch brackets and register
allocation. Neither arm injects traps. This supplied the arithmetic prerequisite
for the completed seven-event trap test above.
The original driver and boot are unchanged; the arithmetic pair's terminal
device checks are idle and its kernel log is byte-identical to its pre-run
snapshot. The later quartet's log additions are recorded above.
[Complete arithmetic matrix](evidence/current_20260919/DW_sink_arithmetic_matrix.json),
[root result and runtime receipts](evidence/current_20260919/DW_sink_arithmetic_result.json).

**Fresh no-injection observations now show the predicted register copies.**
The subsequent fresh-process **NOP-only sweep** contains six predicted
copies with fresh return PCs in 400 calls, while invoking no explicit
calibration kernel anywhere in that process: three bank05 `v257 → v1`
copies and three bank46 `v513 → v257` copies. Six fresh equal-bank
observations retain their tags; 388 calls have no fresh observed in-loop PC.
All 1,200 retained images, 2,410 HIP calls and native/runtime checks pass.
This removes same-process explicit-calibration carryover as a requirement
for these spontaneous copies. The module contains unused calibration
entrypoints, and the earlier explicit calibrations remain separate evidence.
[NOP-only result](evidence/current_20260919/fresh_TTMP_NOP_only_result.json).

The reversed 800-call tagged-register sweep contains two bank05
`v257 → v1` copies and one bank46 `v513 → v257` copy, each with a changed
TTMP return PC inside that invocation's scalar wait loop. Two fresh
equal-bank observations retain their tags, and all 400 explicit-trap
calibrations match. The complete earlier native/runtime audit passes; the
restored session independently rehashed all 2,400 retained images and
reconstructed the tags and PC-freshness predicate. The tag snapshot window
is wider than the TTMP window, so this does not identify one uniquely causal
trap or the original training event.
[Restored raw-byte result](evidence/current_20260919/fresh_TTMP_800_restored.json).

**The combined-trap hardware replay now reproduces every byte of attempt
405's complete DW in four of four calls: all 131,072 BF16 words, including
the 640 NaN payloads, 384 enormous finite values and six small residual
differences.** All four matched no-trap calls equal the complete ordinary
baseline. The frozen full-output criterion and independent native, input,
packet, control and library audits pass, with no outliers in this pair.
The gate selects seven wave events at visit9415: one bank2 → bank1 copy
poisons the persistent X address, and six bank1 → bank0 copies replace
v1 accumulators with tiny address bits. This establishes controlled
sufficiency for the complete observed failure. The original training native
kernel and trap history remain unrecorded; a unique historical event sequence
and a training repair are not established.
[Complete result](evidence/current_20260919/training405_combined_full_result.json),
[independent full matrix](evidence/current_20260919/training405_combined_full_matrix.json).

**Joint prevention also passes:** retaining all seven selected traps and
both trigger PCs, but equalizing the source/destination bank routing, returns
the complete ordinary baseline in all four trapped calls and all four
no-trap calls. Exactly four bank-setting immediate bytes change per binary,
including the corresponding previous-mode fields. The full-output and
native/input/runtime audits pass without an outlier in this pair.
The installed handler is unchanged; this protects the selected controlled
events and does not establish protection from every spontaneous trap or
training stability.
[Joint prevention result](evidence/current_20260919/training405_joint_prevention_result.json),
[independent matrix](evidence/current_20260919/training405_joint_prevention_matrix.json).

**One input bit changes the same defect from NaNs to finite corruption.**
Changing only `DY[602561,194]` from BF16 `0xb101` to `0xb100` on a CPU clone
changes the copied address from odd `0x3176b101` to even `0x3176b100`.
The original single-target NOP/TRAP binaries and installed handler are
unchanged. All four NOP outputs equal the original full baseline; all four
TRAP outputs are identical and entirely finite, with **1,001 changed words**
inside M0:8/N128:256 and zero outside differences. The largest difference
from NOP is `1.3634562492370605e-6`; the full output maximum is
`2.0623207092285156e-5`. All frozen finite/repeat/support criteria and
native/input/runtime audits pass. The wrong outputs remain wrong: this
demonstrates input-bit sensitivity, not a handler repair or the actual
reason for successful 1P4G training.
[One-bit result](evidence/current_20260919/training405_onebit_DY_result.json),
[full matrix](evidence/current_20260919/training405_onebit_DY_matrix.json),
[prelaunch interval comparison](evidence/current_20260919/training405_onebit_interval_comparison.json).

**The preceding selected-trap hardware replay reproduces every bit of attempt
405's 1,024-word corruption block in four of four calls:** all 384 enormous
finite BF16 words and all 640 NaNs, including their `0xffc0` payloads. With
the original saved X and DY, one injected trap in group1/wave2 at loop visit
9415 produces the predicted bank2 → bank1 address copy. Changing the bank
setting around that trap prevents the block in four of four calls and
returns the complete ordinary baseline. Native images, all 172 argument
bytes, complete retained inputs/outputs, controls and runtime libraries pass
their audits. [Controlled result](evidence/current_20260919/training405_selected_trap_result.json).

**That earlier four-arm matrix criterion fails because one no-trap control call
has 11 small differences outside the target block.** Its next three calls
match the baseline exactly. All four corrupting-trap calls match every
outside-block baseline word; all four equal-bank trap calls match the full
baseline. The original training result also has six small outside-block
differences from that controlled corrupting result, now reproduced by the
combined experiment above. The earlier outliers and failed criteria remain
retained. The selected single-trap experiment establishes sufficiency and
prevention of this exact block in the replay; the original training native
kernel and trap history remain unobserved, and no training fix is established.
[Frozen matrix](evidence/current_20260919/training405_selected_trap_matrix.json),
[entire historical DW comparison](evidence/current_20260919/training405_entire_DW_replay_comparison.json).

The complementary **address-consumption test also removes the entire block
in four of four trapped calls**. It retains the bank46 trap but replaces
nine v257 load-address uses with equivalent v256-plus-2048 addresses. Both
NOP and trap arms return the ordinary 1,024 target words in every call.
Seven of eight complete outputs equal the baseline; trap call3 has seven
small outside differences, so this full-pair criterion also fails. The
result supports use of the copied address as the cause of the large block,
while preserving the other observed errors and the limits on historical
attribution. [Mediation matrix](evidence/current_20260919/training405_address_mediation_matrix.json),
[complete target comparison](evidence/current_20260919/training405_address_mediation_target.json).

The earlier completed full-BF16 bank comparison is **FAIL8 / PASS1000**, after both
arms pass a nonzero sparse arithmetic witness. The earlier controlled
actual-DW single-trap matrix also reproduces the predicted diagnostic
corruption under bank81 and suppresses it under equal-bank routing. These
results strengthen the trap-copy lead; a complete training fix remains
unproved. The strongest findings are:

- **Original training block reconstructed from saved bytes:** original
  `DY[602561,194:196]` packs into `0x3176b101`. If this word replaces v257
  lane0 in group1's producer wave2 before load visit9415, the persistent
  address reads **1,576 complete odd-offset X vectors** and then goes out
  of bounds. The A/LDS/WMMA dataflow supplies M0:8 and N128:256. Exact
  dyadic sums and forward/reverse FP32 sums reproduce **all 384 saved finite
  BF16 words**; rows M2/3/4/6/7 acquire NaNs, matching the **640-NaN** block.
  An increasing-K FP32 interval calculation includes the omitted ordinary
  terms and remains inside all 384 saved BF16 rounding cells. The numerical
  model does not determine hardware NaN payloads or uniquely identify a
  trap PC. [Complete evidence binding](evidence/current_20260919/training405_mainloop_copy_complete_binding.json),
  [numerical match](evidence/current_20260919/training405_mainloop_copy_numerical_match.json),
  [ordered rounding enclosure](evidence/current_20260919/training405_mainloop_copy_ordered_rounding.json),
  [independent raw-byte reconstruction](evidence/current_20260919/training405_mainloop_copy_independent.json).

- **Full-BF16 comparison — FAIL8 / PASS1000:** the original BF16 output
  epilogue, B reuse, C accumulation, and all 320 WMMA instructions remain
  present. Both arms first pass four nonzero sparse-input calls against an
  exact oracle. With original X and zero DY, bank81 then fails at call8 with
  **1,024 finite wrong words**, maximum **`3.851859888774472e-33`**; bank82
  passes 1,000 calls. Complete outputs, native launches, all 172 argument
  bytes, captured metadata, seven runtime libraries, and the control's
  complete saved failure inputs pass their audits. The zero harness checks
  and hashes full prefills but does not retain their raw bytes. These tiny
  errors and the enormous training block remain distinct observations.
  [Full-BF16 comparison](evidence/current_20260919/FULL_BF16_bank_comparison.json).

- **Reverse-order repeat — PASS1000 / FAIL70:** after the host reboot and
  restoration of the byte-identical driver, the same equal-bank ELF passes
  1,000 calls, followed by control failure at call70. The failure has
  **272 finite wrong words / 256 sources**, with **16 equal nonzero
  direct/normal pairs** in group3. All four affected waves sample exit setter
  **`0x813c`**, outside the original 14 selected LDS heads; neither new arm
  samples those 14 heads, and the new equal arm has no samples at the seven
  adjacent exit setters either. Full saved-input checks pass. Every wrong
  source fits the historical-v257 donor model exactly and under FP32 RNE,
  at 465 matching saved X rows; the neighboring-v258 model has no matches.
  These bounded repeated observations support the bank comparison without
  establishing a fix or assigning the original training NaNs.
  [Complete repeat result](evidence/current_20260919/BANK_EQUAL_reverse_PASS1000_FAIL70.json),
  [separate exit-setter comparison](evidence/current_20260919/BANK_EQUAL_reverse_exit_PC_comparison.json).

- **Earlier matched bank comparison — FAIL203 / PASS1000:** both arms use the
  same address-copy setup and TTMP observer. The equal-bank arm changes only
  15 LDS address operands and 16 accompanying bank setters, preserving
  ordinary effective addresses, arithmetic and register allocation.
  The control has **272 finite wrong words / 256 source values**, with
  **16 equal nonzero direct/normal pairs**. All four affected group1 waves
  sample **`0x8114`** in a preselected bank81 window; none of the preceding
  202 calls samples the 14 preselected heads. Its wrong products fit the
  historical v257 donor model. The equal-bank arm's native audit verifies
  **all 1,000 outputs and prefills**, with zero arithmetic errors and valid
  metadata. The completed independent PC/source-wave audit checks all
  **16,000 wave snapshots and 65,472,000 arithmetic words**: the equal-bank
  arm has **zero samples at the original 14 selected heads**. It has 180
  separate samples at adjacent exit setters in 45 passing calls. Old absolute
  control PCs also recur in the later process, demonstrating why the current
  module-base join and snapshot-age limits matter.
  This is bounded supporting evidence, not a demonstrated training fix or
  failure-rate estimate.
  [Control native audit](evidence/current_20260919/BANK_EQUAL_control_FAIL203_native.json),
  [control full audit](evidence/current_20260919/BANK_EQUAL_control_FAIL203_full.json),
  [control PC audit](evidence/current_20260919/BANK_EQUAL_control_FAIL203_PC.json),
  [equal-bank native audit](evidence/current_20260919/BANK_EQUAL_equal_PASS1000_native.json),
  [complete matched PC audit](evidence/current_20260919/BANK_EQUAL_complete_PC_comparison.json),
  [snapshot-age and entry-ordering evidence](evidence/current_20260919/BANK_EQUAL_snapshot_age_and_entry_ordering.json).

- **Controlled actual-DW single-trap matrix:** NOP bank81 and NOP bank82 each
  pass four fully retained calls with zero arithmetic and no forced return-PC
  samples. Injected-trap bank81 fails on its first call with **1,088 nonzero
  arithmetic words / 1,024 unique wrong sources / 64 equal nonzero direct-normal
  pairs**, and every arithmetic word matches the frozen predicted corruption
  image exactly. Injected-trap bank82 passes four calls with zero arithmetic
  while all 16 waves in every call report the expected injected return PC
  `0x4468c`. This is an actual-kernel counterfactual for the installed
  trap-copy mechanism; it still does not prove spontaneous trap timing in the
  original training failure or establish a training fix.
  [Gated trap matrix](evidence/current_20260919/gated_actual_DW_trap_matrix.json).

- **Earlier replay/PC match:** unchanged control **FAIL43**; TTMP observer
  **FAIL332**, with valid metadata. All **256 wrong source values** in the
  observer belong to workgroup 2's four waves; all four sample **`0x80d4`**,
  after the v513 LDS-load request and inside a bank-`0x81` window that predicts
  **v257 → v513**. None of the preceding 331 calls samples any of the 14
  preselected instruction heads in those seven windows. The wrong products
  fit the old-v257 donor model. This is a same-invocation PC/geometry match,
  with TTMP age and last-visible-history limits, rather than identification
  of `0x80d4` as the faulty instruction.
  [Completed comparison](evidence/current_20260919/TTMP_pair_result.json).

- **Confirmed controlled mechanism:** a 14-case tagged-register probe shows
  that explicit `s_trap 3` copies historical **v257 lane0 into v1 or v513**
  under the predicted unequal-bank settings. All seven no-trap cases and
  four equal-bank trap cases preserve their tags; all seven trap return PCs
  match exactly. The installed CWSR context-save handler reads and writes
  encoded `v1` before clearing the bank fields, explaining these controlled
  copies. [Controlled result](evidence/current_20260919/CWSR_sentinel_controlled_result.json).
- **Causal provenance in the failing arithmetic replay:** changing historical
  v257 by **−16**, while compensating every effective load address, changes
  the donor bits and BF16 factors that explain the wrong products. Both
  direct VMEM reads and later VALU-copy reads return identical corruption,
  including **32 nonzero pairs per arithmetic event**. This rules out a
  fault confined to the later export copy. The predicted trap-copy geometry
  matches three earlier replay failures. Those older runs had no trap-PC
  observer; the new instrumented replay supplies the PC/geometry match above.
- **Strongest training producer interval:** at attempt **405**, the selected
  preprocessing Linear's raw DW contains **640 NaNs**, **384 finite values
  above `1e6`**, and a maximum of **`1.02204329643401e31`**, despite small,
  finite, unchanged X and DY. The corrupt result propagates bit-for-bit
  through the expected cast and gradient path. There is no native trace at
  this event, so the later replay kernel cannot be assigned retrospectively.
- **Bounded mitigation evidence:** cap256/BN1/workspace1 with private DW
  solution102 completed **1,000 optimizer steps** and passed the complete
  final-frame/native-trace audits. Solution102 also passed two separate
  1,000-call zero controls. These results do not establish long-run stability
  or clear the original defect.

**Repair build update:** the corrected non-wave-start CWSR handler and a
scratch driver module have now been built. The reconstructed baseline matches
all 5,656 installed handler bytes; the correction adds 16 bytes, with all 165
branch targets checked. The rebuilt baseline's 37,959 CPU functions match the
installed driver. The candidate changes only two size-selection immediates in
CPU code; all 827,798 normalized module relocations and 2,930 other read-only
objects pass comparison. LLVM reconstruction preserves 84 installed VOP3
encodings explicitly. This is a complete build, not an SP3 source assembly or
a hardware-validated fix. The original/candidate/restored sentinel comparison
started, but was interrupted after successful original-driver removal at
**02:54:25 UTC**, before any candidate-load completion was recorded. The host
rebooted at approximately **02:59:15 UTC**; the cause is unknown. The original
pre-switch sentinel's complete raw data survived, and its two empty audit
files were reconstructed with their historical contents verified. After this
reboot, the unchanged installed driver was restored, all four data-movement
health arms passed 1,000 calls, and a fresh original-driver sentinel reproduced
the same three predicted cross-bank copies. This recovery does not complete
the driver comparison or supply a corrected-driver runtime result.
[Interruption and recovery](evidence/current_20260919/CWSR_driver_interruption_recovery.json).
An independent load-interface review finds no differences in 1,161 undefined
symbols, 1,162 imported symbol CRCs or 827,798 normalized relocations; it does
not diagnose the interrupted load.
[Load-interface review](evidence/current_20260919/CWSR_interrupted_load_interfaces.json).
The renewed switch audit confirms that `rmmod` returned zero at
02:54:25.009508. Candidate `insmod` was the next programmed operation, but
the controller logged commands only after completion. There is no candidate
load-start/completion, VM/sentinel or restoration receipt, and no useful
September21 pstore record. The interruption is localized to that observation
gap; neither a delayed unload defect nor candidate-load failure is diagnosed.
A fresh-boot first-load baseline/candidate comparison is a possible design,
conditional on external recovery and persistent logging; it is not a
validated activation route.
[Switch reaudit](evidence/current_20260919/CWSR_driver_switch_reaudit.json).
The independently reviewed public registration path replaces the second-level
handler only. CWSR is enabled with a read-only parameter; the selected
first-level mapping lacks GPU write permission, and reviewed status
interfaces do not expose its reserved mapping offset. No supported live
replacement route has been established; internal VMA-access caveats remain.
[API/mapping review](evidence/current_20260919/CWSR_first_level_API_mapping_reaudit.json),
[status-interface addendum](evidence/current_20260919/CWSR_read_only_offset_exposure.json).
[Handler build](evidence/current_20260919/CWSR_complete_handler_build.json),
[module byte audit](evidence/current_20260919/CWSR_module_byte_audit.json),
[module relocation audit](evidence/current_20260919/CWSR_module_relocation_audit.json).

The controlled single-trap test inside the actual DW kernel is now complete.
One TTMP snapshot still cannot recover a complete trap history: its initial
age is unknown, and later traps can hide earlier ones. The completed
final-accumulation reset runs failed in other
accumulator blocks, leaving that earlier C-history discriminator unresolved.
Details of the preceding experiment are under
[the completed TTMP observer](#completed-ttmp-observer).

The detailed evidence below separates current observations from the dated
bring-up record. [Strongest evidence](#strongest-evidence) ·
[component isolation](#continued-component-isolation) ·
[porting changes and reversal criteria](#historical-porting-changes-and-rollback-record) ·
[stack and controls](#stack-and-controls) ·
[evidence and session recovery](#evidence-and-session-recovery).

## Strongest evidence

| Component / observation | Completed result | What remains unproved |
|---|---|---|
| **Complete historical DW reproduced by combined trap copies** | **PASS4 / PASS4** under the frozen full-output criterion: all four NOP calls equal the ordinary baseline; all four trapped calls equal every one of the **131,072 historical BF16 words**, including all NaN payloads and six small residuals. Independent native/packet/input/output/runtime audits pass. | Seven selected wave events are statically derived from the gate, without a runtime trap-count observer. Original training native kernel/trap history and a validated repair remain unproved. Earlier separate matrix failures remain failures. |
| **Joint equal-bank prevention** | **PASS4 / PASS4**: all eight outputs equal the complete normal baseline while the trapped arm retains the same seven selected events and two trigger PCs. Four bank-immediate bytes change per ELF; all arithmetic and native ABI bytes remain unchanged. Full native/input/output audits pass. | Protects the selected gates, not every original unsafe window. No installed-handler repair or training stability claim. |
| **One-bit DY sensitivity** | **PASS4 / PASS4** under frozen finite/repeat/support criteria. A single CPU-cloned DY bit changes the poisoned address from odd to even. Four NOP outputs match the full baseline; four trapped outputs contain the same **1,001 finite wrong words**, confined to the predicted block. Every target word is enclosed by the prelaunch numerical intervals. | Passing this experiment means the predicted corruption is finite and repeatable, not that arithmetic is correct. Actual successful-host inputs and cause remain unobserved; central FP32 model is not an exact hardware oracle. |
| **Original training block and controlled persistent reverse copy** | A selected trap at visit9415 reproduces **all 1,024 original target raw words** in four calls, including **384 enormous finite values and 640 NaNs**. All 130,048 outside words match the NOP46 baseline. Equal-bank trap prevention returns the full baseline in four calls. | This earlier full matrix fails on NOP86 call1's 11 small outside differences; its other three calls match. Its six-word historical residual gap is closed by the combined experiment above. No original training native trace/trap record or complete training fix. |
| **Original full-BF16 bank comparison** | Both nonzero sparse witnesses **PASS4**; zero-DY bank81 **FAIL8** with 1,024 finite wrong words, followed by bank82 **PASS1000**. Complete output/native/packet/runtime audits and failure-input audit pass. | The tested LDS reroute leaves the proposed reverse-copy window intact. Zero-DY passing does not test prevention of the original nonzero-DY NaN mechanism or establish a training fix. |
| **Failing DW replay and sampled trap-return PC** | Control **FAIL43** has 288 bad words; observer **FAIL332** has 272 arithmetic bad words after excluding exactly 64 duplicate metadata slots. Both retain 256 wrong true sources. The observer's four affected waves all sample **`0x80d4`** in the predicted bank81 window; the preceding 331 calls have zero sampled PCs in the 14 preselected heads. Full raw/native/input/PC audits pass; donor products fit historical v257. | TTMP records the last visible value, with age/history and restart limits. The sampled LDS instruction is not assigned as the corrupting instruction. A kernel counterfactual and linkage to original training NaNs remain open. |
| **Controlled installed trap path** | **14 tagged-register cases** reproduce v257 lane0 → v1 for setter `0x05`, and v257 lane0 → v513 for `0x81`/`0x85`; all seven no-trap and four equal-bank trap controls remain unchanged. All seven trap return PCs match exactly; 560 output words independently audited. The actual-DW single-trap matrix then gives NOP PASS4/PASS4, trap bank81 FAIL1 matching the predicted 1,088-word corruption image exactly, and trap bank82 PASS4 with all 64 forced return-PC wave samples present. | Demonstrates the controlled cross-bank copy in the standalone sentinel and in the actual DW replay body. Spontaneous trap timing during original replay/training failures and linkage to training NaNs remain unobserved. |
| **Actual training Linear DW** | Cap256/BN1/workspace1 stops at attempt **405**, after 404 completed steps. Raw BF16 DW has **640 NaNs**, **384 finite values above `1e6`**, maximum **`1.02204329643401e31`**. Complete 38.33 GB CPU audit. | No native trace at this training failure; the later replay kernel cannot be assigned retrospectively. |
| **Isolated Linear DW** | With original X and zero DY, only `torch.mm(DY.T, X).T` fails at call **196**: 512 wrong values, maximum `2.624079549227609e-33`. Complete 10.71 GB CPU audit; all 196 launches use `MT256x128x64`, LDSB1/SK0, 4 workgroups, 676 static VGPRs. Earlier Addmm zero-DY fails at 55. | The tiny replay errors and enormous training errors are distinct observations. Corrupting instruction, device execution and common mechanism are open. |
| **Alternative Linear DW** | Solution **102**, `MT32x16x32`, 252 VGPRs, passes two separate 1,000-call zero controls. Combined cap256/BN1/workspace1/private-DW training completes **1,000 steps**, with a fully finite final capture and **14,000 solution102 / zero solution103** traced launches. | Tile, grid, LDS and code scheduling change with VGPR count. One bounded training pass does not isolate a register threshold or establish a complete fix. |
| **Native DW B-reuse control** | Fresh original fails at **75**; B-reuse-off passes **1,000**; restored original fails at **561**; repeated B-reuse-off fails at **154**. Each failure has **512 CPU-verified finite wrong values**. Only **280 B-reuse hint bits** differ between executables; all target launches use the verified replacement ELF. | B-reuse hints are **not necessary** for this exact-zero defect. The first modified pass was intermittent nonreproduction, not a fix. |
| **Native DW literal-zero B fragments** | With B-reuse disabled, replace **384** fragment writes by literal zero. Two fresh processes each pass **1,000** checks; every modified-ELF launch is verified. The matching new-loader B-off baseline also passes **1,000**. | The changed instructions alter dependencies/timing. These are bounded nonreproductions, not a causal isolation of B production or a training fix. |
| **DW historical register-value leakage** | Raw prepack export fails at **56**, direct accumulator export at **6**, with exact earlier v257 address bits in the v1 observation path. Fresh direct control fails at **804**; changing v257 by **-16** with all 12 load addresses compensated fails at **56** with **all ten wrong words shifted by -16**. Complete payload/stream/native audits pass. | Wrong accumulator contents versus wrong register delivery, stores or copies remain open. The raw subnormal words would round to BF16 zero; a common mechanism with original NaNs is not established. |
| **DW historical-value arithmetic causality** | Matched C0 control **FAIL34** fits only old BF16 donors; the -16 compensated-address arm **FAIL7** fits only tagged donors. Each has 256 wrong source values and 32 equal direct/normal pairs. Full input/packet/prefill audits and exhaustive exact/RNE X-row searches pass. | Different X profiles and events; no same-event 8/9 scaling measurement. The exact corrupting instruction and a common mechanism with training NaNs remain unproved. |
| **DW repeated v513 source-read removal** | Original-read control **FAIL532** repeats the old donor arithmetic in 256 source values and 32 equal pairs. Replacing only source0 at `0x7540` by inline zero gives **PASS1000**; all 65,536,000 retained FP32 words are positive zero. Native, prefill and complete output-stream checks pass. | Inline zero removes a read dependency and can change instruction handling or timing. One passing arm does not establish a fix or uniquely locate the faulty physical component. |
| **Native projection DX** | `torch.mm(DZ, W.T)` gives nonzero DX with exactly zero DZ. Several full CPU-verified failures; 230/256/460-workgroup cases fail, while 16,330/32,660-workgroup and workspace1 controls each pass 1,000 calls. | Persistent multiple-tile execution is a lead. Clearing 392 A-reuse hint bits still fails; hints are unnecessary for the observed error. |
| **Production attention DQ** | Exact-zero reduced replay fails; cap256 passes 1,000 raw calls, then uncapped fails again at 93. All four full 40-site WMMA padding arms also fail. | No faulty WMMA/register instruction or causal link to enormous Linear DW established. Cap256 does not fix B0 training. |
| **Weighted input LN** | B0 BN8 fails zero-DY calls 115/212 around a BN1 1,000-call pass. A0 BN8/BN1/BN8 each pass 1,000 calls. B0 BN1+cap256 still fails training. | The A0 passes do not clear B0's intermittent component failure or identify a common cause. |
| **Observation inconsistency** | Earlier uncapped capture stops on a comparison flag although every saved tensor is finite and relevant bytes match; 113/163 scalar joins disagree with CPU. Current smoke's retained scalar stages agree. | A scalar alarm alone cannot identify corruption. Audit saved raw tensors and the actual scalar-stage chain separately. |

## Where the enormous value first appears

Attempt 405 captures X `[2324351,256]`, maximum `0.0048828125`, and DY
`[2324351,512]`, maximum `0.000885009765625`. Both are finite and their complete
pre/post backing bytes match. Raw DW `[256,512]`, stride `[1,256]`, contains
the corrupt block `[0:8,128:256]`. DX/DB and retained top-layer projection
inputs/outputs remain finite.

The expected transpose and BF16→FP32 conversion, leaf gradient, accumulated
gradient, backward-return gradient and final selected dense gradient are
**bit-exact**, including the NaNs. This is before clipping and the explicit
optimizer step; in-backward sparse updates may already have occurred.

At one captured `+7.9723e29` coordinate, an exact-integer sum of all 2,324,351
BF16 products is **`-4.1748697645262154e-7`**. The exact-real global magnitude
bound is 10.0443; a conservative ordinary FP32 accumulation bound is 13.8942.
Ordinary accumulation overflow cannot explain this event under that model.

The historical `1e37`–`1e38` events are still unlinked. B0's captured layer
norm and jagged split receive already-enormous inputs; they establish
propagation, not the original producer. A0's earlier step-380 GEMM similarly
receives bad DOut before executing. The attempt-405 raw-DW capture is a newer,
stronger producing interval, not proof that every earlier event shares it.

Primary raw-DW evidence: [complete attempt-405 CPU audit](evidence/current_20260919/training405_full_payload.json)
and [isolated call-196 CPU audit](evidence/current_20260919/single_DW196_full_failure.json).
The retained X-row/common-scalar donor search excludes that specific model
for the wrong replay columns. It does not exclude multirow sums, remapping
or changed intermediate arithmetic.

### Persistent address-copy reconstruction of attempt 405

The current numerical lead connects the confirmed CWSR copy defect to the
original values through a different bank window from the earlier zero-DY
diagnostic failures. With source bank2 and destination bank1, an encoded-v1
read/write in the handler can copy **v513 → v257**. In the replay kernel,
v513 holds packed DY while v257 supplies a persistent X buffer offset.
The existing bank82 LDS reroute addresses the opposite **v257 → v513**
windows; it leaves this proposed reverse-copy window intact.

An exhaustive source/bounds scan followed by the finite-output sign screen
isolates a candidate first affected load at visit **9415**, with loop counter
**s12=26902**. The two source halfwords are original DY **`0xb101` and
`0x3176`**, at K602561 and N194/195. Their packed word **`0x3176b101`** is
829,862,145 bytes, an odd address offset. The first resulting X byte address
is 1,138,438,385 relative to X's base. The copied offset remains live while
the descriptor base advances by 32,768 bytes per visit. Loads through visit
10990 are fully in bounds; subsequent loads are fully outside the resource.
The [source and descriptor audit](evidence/current_20260919/training405_mainloop_source_and_SRD.json)
checks both source halfwords and all 145,256 group/visit descriptor states at
the actual load instruction, after descriptor packing.

The independent dataflow maps each loaded 16-byte vector to X features M0:8,
then through shared LDS to all 128 output columns of group1. Spliced BF16
coefficients become NaN in exactly M2/3/4/6/7. For M0/1/5, their products
with the unchanged saved DY reconstruct every finite saved BF16 word.
An ordered FP32 interval calculation permits all ordinary terms between
changed coefficients and still leaves a minimum **`5.1379e24`** margin to
the saved BF16 rounding boundaries. This calculation assumes increasing-K
FP32 RNE/FMA or equivalent WMMA accumulation; it does not measure native
WMMA internal behavior. An [independent integer calculation](evidence/current_20260919/training405_mainloop_rounding_independent.json)
reproduces every interval endpoint. The [native temporal/dataflow audit](evidence/current_20260919/training405_mainloop_temporal_order.json)
confirms one changed product per 64-K tile in increasing K, with matching
A/B K indices and the same C accumulator chain.

A nine-case hardware probe uses the exact buffer-load instruction encoding,
physical register banks, and descriptor format. Offsets 1/2/3 return exact
shifted bytes. Boundary observations match checks in eight-byte components,
an interpretation added after observing the retained data. The earlier
four-byte model and this eight-byte model give the same candidate timeline
because it contains no partial final load.
[Complete hardware observation and scope](evidence/current_20260919/buffer_unaligned_QWORD_hardware.json).

Two tail alternatives were excluded before this main-loop match: bank0 →
bank2 copies into v513 occur after its last relevant read and are overwritten
before reuse; the first tail accumulator → v257 copy cannot manufacture
large or NaN coefficients from the small remaining X resource under ordinary
accumulator prehistory.
[Tail v513 lifetime](evidence/current_20260919/DW_tail_v513_deadness.json),
[tail address-copy exclusion](evidence/current_20260919/DW_tail_v257_copy_exclusion.json).

The original 640 NaNs all have raw word `0xffc0`; the CPU reconstruction
currently establishes their locations and classification, not the hardware
NaN sign/payload. Several bank2 → bank1 windows could supply the same word
before the first affected load, so visit9415 does not uniquely locate the
original trap PC. Later windows holding the same DY word can instead make
visit9416 the first affected load; dropping the first modeled contribution
still reproduces the saved finite words and NaN rows. Both complete timing
screens test 183 mask-compatible wave subsets and reject 179 by finite signs;
the same four cases survive at source visit9415, each including wave2 with
optional waves0/1. The [window and timing scope](evidence/current_20260919/training405_mainloop_lag_and_window_scope.json)
records nine reverse-copy windows before the load and three after it but
before the next source refill. A controlled trap at `0x7540`, with NOP and
prevention controls, has now completed. The cave gate selects only
group1/wave2 at visit9415 and leaves all 320 original WMMA instructions and
the complete BF16 epilogue intact. All four bank46 trap outputs reproduce
the 1,024 target raw words exactly; all four bank86 trap outputs equal the
complete first NOP46 output. The full frozen matrix still fails because
the first NOP86 call differs at 11 outside words; its remaining calls and
all four NOP46 calls agree. The six additional small differences in the
historical DW are reproduced by the subsequent combined experiment below.
See the [complete controlled result](evidence/current_20260919/training405_selected_trap_result.json).
The original training kernel and trap history remain unobserved. The separate
nine-load address substitution has now returned all 1,024 ordinary target
words in every NOP and trap call while retaining the bank46 trap. Its third
trap call has seven small outside differences; the full-pair criterion fails.
Both interventions prevent the large block in these bounded calls, through
different parts of the proposed causal chain.

The six residual coordinates are M8 at N0/64/256/320 and M136 at N256/320.
All map to physical v1 lane0. The
[exact dyadic suffix calculation](evidence/current_20260919/training405_residual6_exact_prediction.json)
and seven scoped FP32 schedules predict their historical values when the
earlier v1 accumulation is replaced by the tiny v257 address donor near the
same visit9415. The selected `0x7540` site leaves the suffix beginning at
K602592. A combined gate retains the group1/wave2 bank46 trap and adds
bank05 traps for group0/waves0,2 and all four group2 waves, restoring bank46
before the original displaced instructions. All 320 WMMA instructions,
the BF16 epilogue and native ABI remain unchanged.

The combined hardware experiment **passes the frozen full-output criterion**:
all four NOP calls equal the ordinary baseline
`da3cc5656e1b105fc638d5556399233140fd2e5542ef822b5d7e9b4d8275d11e`;
all four trapped calls equal the historical full DW
`7131a70208bc8432aca19688eb96e75e04779ab81b265adc120758b197a233d9`.
The independent comparator checks all 262,144 bytes per output against
references bound to the original archive member and tensor metadata.
There are no additional mismatches in this pair. This closes the six-word
reproduction gap without changing the failed verdicts of the preceding
four-arm or mediation experiments.

The subsequent joint-prevention pair retains all seven traps and both
trigger PCs. Residual routing changes bank05→bank00, making the handler
restore v1 to itself; address routing changes bank46→bank86, restoring v513
to itself. Matching previous-mode fields are updated, and both paths restore
bank46 before the displaced original instructions. Only four immediate
bytes differ from each corresponding combined binary. All four NOP and all
four trap outputs equal the complete ordinary baseline; the independently
audited frozen matrix passes with zero mismatches. Other unsafe windows in
the original kernel remain, so this is controlled prevention rather than
a general kernel or driver repair.

A separate [training-DY row search](evidence/current_20260919/training405_DY_row_scalar_search.json)
tests each of the three huge finite DW row slices, M=0,1,5 and N128:256,
against every one of the 2,324,351 original DY rows. None has the necessary
sign pattern for multiplication by a scalar of either sign. An
[independent complete-vector scan](evidence/current_20260919/training405_DY_row_scalar_independent.json)
confirms this rejection. The other five affected rows contain 640 `0xffc0`
NaNs and are outside this finite-scalar model. This excludes only one saved
DY row times one finite real scalar for each tested slice; it assigns no
kernel or common cause to the untraced training event. A
[bounded-residual extension](evidence/current_20260919/training405_DY_row_bounded_residual.json)
with an [independent exact bound review](evidence/current_20260919/training405_residual_bound_review.json)
also allows arbitrary coordinate residuals up to the absolute sum bound for
the other K−1 original terms: **10.0442991**. Every target rounding interval
lies more than `6.2e27` from zero, so those residuals cannot change the necessary
signs. This also rejects one changed X coefficient plus an otherwise exact
real contraction and final BF16-RNE cast. Intermediate arithmetic faults and
other rounding models remain outside this result.

A [rank test](evidence/current_20260919/training405_shared_two_rows_rank.json)
also rejects two shared DY rows with independent coefficients for each of the
three finite output rows, allowing the same residual bound. For M=0,1,5 and
N=128,129,130, every possible matrix within the BF16 rounding cells expanded
by 11 has a nonzero determinant. An
[independent interval calculation](evidence/current_20260919/training405_shared_two_rows_rank_independent.json)
encloses that determinant between `4.9204e91` and `5.0623e91`. A combination
of two shared row vectors would have rank at most two and determinant zero.
This does not exclude three or more shared rows, different source pairs for
each output row, remapping or unbounded intermediate arithmetic errors.

## Completed combined training capture

Run stem: `train_linear_projection_diagnostics_cap256_bn1_workspace1_privateDW_start0_1000_v1`.
The run reached **1,000 completed steps at approximately 10:19 UTC**, with
`status=bounded_complete`, attempt/completed count 1000 and the intentional
trainer exit code 42. The loss logged at sample 1,024,000 is
`0.13777171075344086`. No first-flag stop occurred. The old
[running snapshot](evidence/current_20260919/current_training_snapshot.json)
is historical; it no longer describes an active workload.

The complete **38,556,889,443-byte** finite step-1000 frame has SHA256
`03fc40da8ed904b4dbfc938fcb771e70b48d54d224bfebe5e34f85c9f6a2ea6e`.
CPU scanning verifies **534 logical views / 466 stores**, all finite and
below `1e6`; all six protected input pairs and eight gradient transitions
match bitwise. All **367** scalar-stage rows agree, including **138** complete
final-dense CPU extrema. Of those 367 rows, **163** join their retained
same-observation raw tensors; 204 lack such full-tensor counterparts.
The retained frame is after backward and before the last explicit optimizer
update; terminal control-flow and log evidence separately certify that update.

The complete trace contains **57,000 native launches**, **14,000 solution102**
and **zero solution103**, with matching successful native API returns. All
A/B/C/D pointers uniquely join the selected final Linear DW to launch
**56,990**, M256/N512/K2,338,476, solution102, 256 workgroups. Matching the
output address alone is insufficient. Trace dispatch is distinct from the
numerical evidence supplied by the saved frame.

The failed attempt-405 run and this pass have identical gin, runtime fields
and all **248 common source hashes**. Private catalog selection, host native
tracing, the verification wrapper and the finite-save step differ. These
fresh processes are not an equal-state, single-variable causal experiment.
Resume certificates are under L with stem `private_DW_training1000_`;
the compact [resume evidence](evidence/current_20260919/resume_summary.json)
pins the full audits and the context comparison.

The combined controls are attention cap256, weighted-LN BN1, workspace1,
and one private BF16 `Ailk_Bjlk` catalog leaf redirected from solution 103
to 102. All 35 entries in that table now reference 102; the other decompressed
catalog bytes and native objects remain unchanged. This is not a global
lower-register policy, and compatibility predicates still apply.

A later complete static review gives a mechanism-specific reason to consider
solution102 less exposed to the confirmed copy defect. Its 4,037 decoded
instructions contain **74 bank setters, all zero**, and no write to MODE
that changes the bank fields. Once a zero setter executes, paths confined
to this body preserve equal source0/destination banks, so the handler reads
and restores the same physical v1. Independent decoding binds all 22,568
instruction bytes, the original ELF, kernel descriptor and metadata.
The highest explicit VGPR is v73 despite metadata reserving 252; the
generator's reason for that reservation is unproved. This conclusion comes
from the emitted bank settings rather than the declared register count.
Initial MODE before the first setter and dynamic indirect targets are not
resolved by this static certificate, and other defects remain possible.
[Complete body review](evidence/current_20260919/solution102_bank_zero_body.json),
[independent review](evidence/current_20260919/solution102_bank_zero_independent.json).

A separate original-DY solution102 observation is finite. Its full-K FP64
reference gives maximum absolute difference `5.786662636858106e-8` and
1,357 differences from BF16-rounded reference. Those are measured rounding
differences, not failures under a predefined GPU tolerance; this one call
does not establish repeated original-input stability. See the
[numerical reference review](evidence/current_20260919/solution102_original_DY_reference.json).

The separate private-path single-DW control passes **1,000 exact-zero checks
in 96.036564 s**, with unchanged prepared inputs. All 1,000 traced launches
are solution102, 256 workgroups × 64 threads. See its
[independent numeric/context/trace certificate](evidence/current_20260919/private_DW_control1000.json).
Passing output tensors were not all retained; this verifies their saved
checks and source semantics. There is no direct catalog-file-open trace.

The preceding **two-step smoke completes in 73.419239 s**. Full CPU scanning
of its **23,469,148,365-byte** step-1 frame finds 534 logical views/465 stores
finite and below `1e6`; six input pairs and eight gradient transitions match.
All 367 scalar endpoint/batch/returned/summary rows agree bitwise; 138 complete
final-dense CPU extrema agree. Of all scalar rows, 163 also match retained
same-observation raw tensors; 204 lack such full-tensor counterparts.

All A/B/C/D pointers and dimensions uniquely join the selected saved Linear
DW to **native launch 47**, solution102, M256/N512/K1,415,093. Its output address
alone also matches earlier launch43 because storage was reused. Read the
[pointer/scalar/full-CPU join](evidence/current_20260919/smoke2_pointer_join.json)
and [smoke source/context audit](evidence/current_20260919/smoke2_context_archive.json).
Two finite smoke steps do not establish long-run correctness.

The [training launch manifest](evidence/current_20260919/private_DW_training_launch_manifest.json)
pins 38 files and 39 source-archive members. The smoke independently verifies
260 context source hashes and 262 worker archive/current files. The wrapper
records the private path, manifest/catalog identity and checks around bootstrap.
Its 14 file-only tests, actual 402-entry verifier and three complementary
stage-composition checks passed before the run. Existing numerical producers,
queue and first-trigger rules are retained.

## Reproduce the isolated Linear DW failure

<a id="29-current-attention-reproducer-and-attribution-2026-09-19"></a>
The preferred short reproducer is
[`repro_linear_dw_saved_orientation.py`](../../scripts/repro_linear_dw_saved_orientation.py).
It executes only `torch.mm(DY.T, X).T`; the old `X.T @ DY` expression takes a
different route/layout. The runner zeroes DY, checks every output magnitude
bit (including subnormals), accepts either signed zero, validates input bytes
and retains the first failed output without rerunning the producer.

Set these path abbreviations when resuming on A0:

```bash
A0_LOGS=/home/chcai/mi450_logs/root_cause_20260919
A0_DATA=/data/mlperf_dlrm_v4/root_cause_20260919
```

Check device ownership before launching another workload:

```bash
sudo -n python3 "$A0_LOGS/assert_gpu_idle.py"
```

When the GPU is idle, the following is the
ordinary-selection replay template. **Choose unused output paths**; the runner
rejects existing reports, failure files and temporary files. The actual
post-restore environment and native trace must accompany a causal comparison.
The zero oracle is deterministic; fault occurrence and onset are intermittent,
so a bounded replay can pass without clearing the defect.

```bash
sudo -n python3 "$A0_LOGS/assert_gpu_idle.py"
sha256sum -c "$A0_LOGS/step405_linear_single_dw_frozen_sources_v1.sha256"
docker exec -w /workspace/recommendation \
  -e AMDGCN_USE_BUFFER_OPS=0 -e TRITON_FULL_AUTOTUNE=0 \
  -e TRITON_ALLOW_PIPELINING=0 -e HIPBLASLT_WORKSPACE_SIZE=1 \
  -e HSTU_BWD_MAX_VGPR=256 -e WEIGHTED_LN_BWD_BLOCK_N=1 \
  triton-7ff97e-20260910 env \
  -u TENSILE_SOLUTION_INDEX -u HIPBLASLT_TUNING_OVERRIDE_FILE \
  -u TENSILE_STREAMK_FIXED_GRID -u TENSILE_STREAMK_DATA_PARALLEL \
  -u CUBLASLT_WORKSPACE_SIZE -u PYTORCH_ALLOC_CONF \
  LD_PRELOAD="$A0_DATA/trace_projection_hip_launches_v2.so" \
  PROJECTION_HIP_LAUNCH_TRACE="$A0_DATA/step405_DW_recheck_hip.jsonl" \
  python -B -u scripts/repro_linear_dw_saved_orientation.py \
  "$A0_DATA/step405_linear_saved_operands_root_v1.pt" \
  --context "$A0_DATA/train_linear_projection_diagnostics_cap256_bn1_workspace1_start0_1000_v1/context.json" \
  --report "$A0_DATA/step405_DW_recheck.json" \
  --failure-dump "$A0_DATA/step405_DW_recheck_failure.pt" \
  --gpu --repeats 1000
```

The exact historical launcher is `L/step405_linear_single_dw_zero_workspace1_argtrace1000_v1.sh`;
its original output paths already exist. The saved operand bundle is
3,570,835,225 bytes, SHA256 `cc6c966297fd7ad537adc574c470ac9fba70d3891b27773ee3df3a8487a8ce1a`.
The runner restores `context.environment`, including the library path.
A private-catalog comparison therefore needs the separately pinned derived
context, not just a changed shell variable. `TENSILE_SOLUTION_INDEX` is
bypassed by this normal heuristic route; the supported tuning-file route
and actual catalog selection are established by retained native traces.

Other focused reproducers remain in `recommendation/scripts`:
`repro_projection_dx_zero.py` for native projection DX,
`repro_hstu_attention_capture.py` for attention, and
`repro_weighted_layer_norm_zero_dy.py` for weighted LN. Use the corresponding
original frozen launcher/context under L, not the Linear-DW options above.

## Native DW reuse experiment completed at resume

The independently reviewed loader and ELF mutation were exercised in four
serial fresh processes after the post-boot health probe. Same original
saved X, zero DY, context, kernel, M256/N512/K2,324,351, four workgroups,
128 threads per workgroup and **676 static VGPRs** throughout:

| Execution order | ELF | Result | Full saved DW audit |
|---|---|---|---|
| 1 | Original | Fail **75** | 512 finite nonzeros; 7 subnormals; columns 2/66 |
| 2 | B-reuse off | Pass **1,000** | Runtime zero checks and trace audited; no passing output tensor retained |
| 3 | Original restored | Fail **561** | 512 finite nonzeros; 6 subnormals; columns 2/66 |
| 4 | B-reuse off, repeated | Fail **154** | 512 finite nonzeros; 4 subnormals; columns 386/450 |

Each failing column spans all 256 rows. Every complete saved input backing
matches its original/prepared counterpart; both retained DY copies are
exact positive zero. The largest wrong magnitude is approximately `3.9e-33`.
The full 10.71 GB failure bundles and 3.57 GB original operands were audited
on CPU. All launches join the SHA-verified owned replacement ELF/function
and exact argument dimensions/strides. The replay does not retain resident
GPU pointer metadata in its tensor payload, so no payload-to-device-address
certificate is claimed; trace address invariants and call order are verified.

The original and modified ELF differ by exactly **280 cleared B-reuse bits**;
all other bytes and decoded operands remain unchanged. The modified positive
failure establishes that **B-reuse hints are unnecessary for the observed DW
defect**. Its first 1,000-call pass cannot be interpreted as a repair.
Together with the earlier projection A-reuse-off failure, this rules out two
specific hint-necessity hypotheses; it does not isolate a shared instruction.

All three new failures and historical call196 retain column offsets **2/66
within a 128-column tile**, while the affected tile changes. Their values
are mostly different after tile alignment. The replay supports do not overlap
the attempt-405 training anomaly block at rows0–7/columns128–255; the tiny
replay defects still lack a mechanistic link to those NaNs/enormous values.

The [resume evidence](evidence/current_20260919/resume_summary.json) pins the
terminal results, full CPU and strict trace audits, and pattern comparisons.
Reproduction packages are `L/session_resume_DW_pair_v1` and
`L/session_resume_DW_pair_v2`; their named output paths already exist and must
not be reused. All four workers exited and were reaped.

### Literal-zero B-fragment follow-up

A separate diagnostic keeps B-reuse disabled and replaces **320 permutation
writes plus 64 tail-mask writes** with same-size literal-zero moves. This
changes **2,240 bytes at 384 instructions**; all other ELF bytes, instruction
addresses, metadata, **676 VGPRs**, and all **320 WMMA instructions** are
unchanged from B-off. Candidate SHA256 is
`4e94f696024576a8d81389998a272ba0b7e42c4a27845b53cd241230eaf4839d`.

Independent control-flow and backwards last-writer proofs cover **2,560
B-register reads**, with full wave32 EXEC at all 384 writes/320 consumers.
The initial CFG analysis missed underscored indirect-PC opcodes and was
explicitly retracted before any candidate launch. Corrected v2 resolves
672 activation calls, 12 leaf returns and 15 syntactic long jumps, with three
statically false sign branches excluded. The tail replacements complete the proof:
the 320-permutation-only proposal leaves 32 WMMA sites unproved. Separate
assembler/decoder, bank-semantics, full-byte, loader and launch reviews pass.

Using the same dedicated loader and unchanged saved-input runner:

| Serial arm | Completed result |
|---|---|
| B-reuse-off baseline | **1,000 zero checks**, bounded pass |
| B-reuse-off plus literal-zero B fragments | **1,000 zero checks**, bounded pass |
| Literal-zero B fragments, fresh-process repeat | **1,000 zero checks**, bounded pass |

All three terminal results and all 3,000 native launches are independently
audited. Passing output tensors were not retained, so these certificates join
the frozen runtime bitwise-zero checks and native dispatch rather than a CPU
rescan of every output. All workers exited and were reaped.

Architectural zero definitions do not observe actual physical register values.
Replacing permutations/masks also changes dependencies and timing, and the
same baseline ELF has both passing runs and the earlier audited failure154.
Thus the two modified passes do not establish B production as the culprit.
The useful next boundary is B-fragment delivery versus accumulator/output
state, retaining positive controls and full CPU verification of any failure.
Packages are `L/session_resume_DW_Bzero_pair_v1` and `..._v2`; all their named
output paths are used. Their readiness, execution and independent certificates
are pinned in the resume summary. This is a diagnostic mutation, not a
production arithmetic change.

## Continued component isolation

The explicit-output control uses `torch.mm(DY.T, X, out=output).T`, preserving
the saved orientation, original X and zero DY. Before each multiplication,
the complete output is filled with zero or one and read back on CPU to verify
every byte. The first complete CPU output snapshot is retained for checking
and failure capture; later GPU readbacks cannot replace that observation.

| Fresh process | First failure | CPU result |
|---|---|---|
| Output prefilled with zero | **56** | 512 finite nonzeros, 8 subnormals, columns 258/322, maximum `3.659266894335748e-33` |
| Output prefilled with one | **74** | 512 finite nonzeros, 4 subnormals, columns 386/450, maximum `3.538896272811546e-33`; no surviving ones |

Both use the same B-reuse-off ELF, M256/N512/K2,324,351, four workgroups,
128 threads and 676 VGPRs. Every launch joins the recorded A/B/C/D pointers
to the verified native module and full argument packet. All 56/74 complete
CPU output snapshots were independently checked. Full CPU audits cover each
failure's 12 stores and the original bundle's 5 stores: original/prepared/current
X and mat2 match exactly; prepared/current DY is entirely positive zero;
the saved DW matches the first CPU output snapshot byte for byte.

These failures exclude the wrong values simply being previous contents left
untouched in D. They do not exclude wrong writes, register delivery or transfer
corruption. The explicit-output API, prefill and CPU synchronization differ
from the historical runner. No agreement with a later GPU reread is claimed.
Both workers are terminal and reaped. Evidence is under
`L/session_persist_DW_output_pair_v1` and
`L/session_persist_DW_output_failure_audit_v1`.

Static mapping places the recurring column offsets 2/66 in accumulator slice
v128:v191, lanes 0/16 across all four waves, fed by main-loop B v494:v501 and
tail B v492:v499. Independent byte/dataflow analysis also supports a diagnostic
that exports selected FP32 values immediately before BF16 packing, through
the existing stores. This point is **after alpha, LDS scale/bias and activation**;
it is not a direct pre-alpha accumulator observation. The raw32 oracle must
reject `0x00008000` and `0x80008000`, which a BF16 numeric-zero check would miss.
Full-path/EXEC and observer review completed before the matched raw-export
pair ran. With original `torch.mm(DY.T, X).T` allocation and the first CPU
snapshot retained, the B-reuse-off baseline failed at **778** with 512 finite
nonzeros (columns 130/194, maximum `4.574083617919685e-33`). The even-M raw
prepack export failed at **56** with **nine FP32 subnormal words**:

| Original M | Original N | Raw FP32 bits |
|---:|---|---|
| 8 | 128, 256, 384 | `0x00000810` |
| 136 | 256, 384 | `0x00000a10` |
| 8 | 320, 448 | `0x00000c10` |
| 136 | 320, 448 | `0x00000e10` |

All other 65,527 raw FP32 words are positive zero. Complete CPU storage,
stream and native packet/pointer audits pass for both arms. The raw failure
is pinned by the [complete raw-export audit](evidence/current_20260919/raw_prepack_FAIL56_full_failure.json), SHA256
`6795fe74357979e9bb7381cb999ebcda017d8499a6b71461cc184bda8d5771f9`
under `L/session_persist_DW_raw_failure_audit_v1`.

Independent mapping puts all nine words at lane 0, original accumulator v1,
alpha-copy PC `0x1fee0`, prepack v288, raw move PC `0x201e4`, and word 0 of
store `0x2020c`. They exactly match the **earlier K-loop address value**
`v257 = 16 * local_thread_id + 0x810`. Output setup overwrites v257 with
the global N base before the alpha copy, so a simple swap with the live
epilogue v257 does **not** explain the values. Bank0 v1, bank1 v257 and the
previous B-source hypothesis bank2 v513 share encoded register index 1.
This makes stale register delivery or bank selection a concrete hypothesis,
without establishing the faulty instruction or hardware component.

The nine subnormals would round to BF16 zero in the original pack; this
particular raw observation is therefore not yet the original NaN mechanism.
Raw moves, stores and CPU transfer remain possible fault sites. The matched
direct-accumulator pair has now completed: its BF16 baseline failed at **197**
(512 finite nonzeros, columns 130/194), and its direct even-M accumulator
export failed at **6**. That export retained five wrong FP32 words:
`(M8,N128)=0x810`, `(M136,N128)=0xa10`, `(M8,N192)=0xc10`,
`(M136,N192)=0xe10`, and `(M136,N256)=0xa10`; every other word was positive
zero. Both complete failure audits and all stream/native pointer joins pass.
The [complete direct-export audit](evidence/current_20260919/accumulator_FAIL6_full_failure.json) SHA256 is
`dc0ca9afc942930a658ebc17ac226794c492dd2c7689d8d744437c310d203b61`,
under `L/session_persist_DW_accumulator_failure_audit_v1`.

The direct candidate exports original bank0 accumulator v1 into bank1 v288;
alpha, LDS scale/bias and conversion results do not feed those words.
This reproduces the same historical address fingerprint without those
computed values. It still cannot separate wrong accumulator contents from
wrong register delivery during the observation moves, stores or CPU copy.
The [kernel descriptor preserves FP32 subnormals](evidence/current_20260919/FP32_denorm_descriptor.json), so survival of these tiny
words through identity arithmetic is architecturally possible.

A CPU-reviewed counterfactual reduces historical v257 by 16 and adds 16
to all 12 consuming buffer-load immediate offsets, retaining every effective
address and checked bound. The predicted donor words become
`0x800/0xa00/0xc00/0xe00`. **That predicted shift reproduced.** The fresh
direct control failed at **804** with nine old-pattern words; the tagged arm
failed at **56** with ten words, all exactly the new predicted values:
M8/N0,256=`0x800`; M136/N0,256,384=`0xa00`;
M8/N64,320,448=`0xc00`; M136/N64,320=`0xe00`.
Every other FP32 word was positive zero. Full audits confirm unchanged
inputs, every retained CPU output, and all native packet/pointer joins.
Tagged failure audit SHA256:
`e50b082c7d57afd114cebbba4051b6ffbaaecdbf092d10cba21d7154b5208f25`.
The frozen pair is `L/session_persist_DW_donor_pair_v1`, manifest SHA256
`82283745f4ad58f42314a17694a68958690bc55ded70abbc3df34f2bf41d4df5`.

The output follows the changed historical **register value**, while the
effective addresses remain unchanged. This is causal provenance evidence
for that value entering the v1 observation path. It does not establish the
instruction or physical mechanism. [Exact-path review](evidence/current_20260919/first_accumulator_definition.json)
shows that s12 is 36,317: both explicit v1-zero sites are skipped, but all
16 group/wave contexts reach the first WMMA at `0x69bc` with full EXEC and
literal-zero C, writing v0:v7. Thus a one-time misdirected write during donor
construction alone would be overwritten by correctly executed later arithmetic.

The next matched pair, `L/session_persist_DW_direct_store_pair_v1`, changes
three bytes so the first existing store reads bank0 acc v0:v3 directly, then
restores the original bank controls. The usual later VALU-copy/store of v1
remains. For each wave/lane, the same v1 is observed at carrier Mbase+2 by
direct store and Mbase+8 by the ordinary route in the **same invocation**.
The mapped v2/v3 pairs supply sibling controls. The candidate remaps 1,536
carrier words; CPU auditors explicitly distinguish carrier and true source
coordinates.

This pair is now complete. The control failed at **828** with eight ordinary
v1-path words; the direct-store candidate failed at **11** with **20 nonzero
words forming ten bit-identical v1 pairs**. Direct carrier M2/M130 and
ordinary carrier M8/M136 contain the same `0x810/0xa10/0xc10/0xe10` historical
address values. No pair is asymmetric or unequal. All other 502 v1 pairs,
and all 512 pairs each for v2/v3, are zero. The
[full failure audit](evidence/current_20260919/direct_store_FAIL11_full_failure.json)
and [independent complete-stream pair review](evidence/current_20260919/direct_store_FAIL11_pair_review.json)
pass; they check all inputs, 11 retained snapshots, native joins and 16,896
paired observations. Candidate audit SHA256:
`ecb909829f0db68ab80ec1ecdabec05bb97218ce3ec5b5ac3a6596b17c872e6f`.

A fault confined to the later VALU export copy or its normal store cannot
explain this event. Shared accumulator state or register delivery remains
possible; physical register storage is not uniquely established.

The final-tail C-zero pair has now completed. Its fresh control failed at
**4**, with eight more bit-identical historical-v1 pairs. The candidate changes
only the C operand at `0xf4f8` to literal zero on both tail visits, resetting
v0:v7 while preserving A/B, destination and both observation paths. It failed
at **91**, with **256 wrong FP32 values in a different accumulator block**.
All 139,776 v1/v2/v3 pairs across those 91 invocations were positive zero.
The [complete pair audit](evidence/current_20260919/tail_C0_pair_result.json)
and [candidate payload audit](evidence/current_20260919/tail_C0_FAIL91_full_failure.json)
pass. This is a run truncated by another failure, so it does not resolve
whether the historical-v1 corruption survives the targeted C reset.

The new failure occupies every even M at N386/450. Each of its 128 row pairs
satisfies `value(M,450) = 256 * value(M,386)` exactly in FP32; all 256 values
also survive BF16 rounding. [Independent instruction mapping](evidence/current_20260919/tail_C0_FAIL91_source_mapping.json)
places them in v128:v135, v144:v151, v160:v167 and v176:v183. Their final
WMMA updates at `0xf578/0xf588/0xf598/0xf5a8` retain their original C inputs;
none overlaps the reset v0:v7. This exposes a BF16-visible two-column pattern
through the raw accumulator-export path, unlike the tiny historical-v257
words that round to zero. Direct-store pairing has not yet observed these
affected registers, and a shared cause with training NaNs remains unproved.

An exact C-zero candidate repeat failed at **183**, again outside v0:v7,
with 256 wrong words at N258/322. All 281,088 targeted pair observations
through that run were positive zero. Its
[full audit](evidence/current_20260919/tail_C0_repeat_FAIL183_full_failure.json)
passes. These two separate runs ended before the historical-v1 symptom
recurred, leaving the requested C-history discriminator unresolved.

Both raw failures now admit a more specific arithmetic model. For each wrong
coordinate, let `wave = int(M >= 128) + 2 * int(N is the higher bad column)`.
Then every retained FP32 value equals exactly, as a real dyadic number,
`BF16(X[k,M]) * BF16_bits(0x0810 + 0x0200 * wave)`. This uses the historical
lane-0 v257 value for both affected accumulator lanes 0 and 16.
[Independent integer-only verification](evidence/current_20260919/historical_donor_exact_products_independent.json)
checks all 512 wrong words and exhaustively scans all 2,324,351 saved X rows.
It finds 155 matching k indices for FAIL91 and 417 for FAIL183. The
[separate FAIL91 proof](evidence/current_20260919/tail_C0_FAIL91_exact_donor_X_products.json)
also establishes that its 155 matches are identical full 256-feature X rows.
The models using each affected lane's own donor, or a uniform factor across M,
have no matching saved row for either failure. Earlier exclusions of a common
scalar or particular two-row/interval families did not cover this model.

The [nine-case BF16 breadth audit](evidence/current_20260919/BF16_donor_model_nine_failures.json)
then finds the same model fits all 512 stored BF16 words in each older event:
Addmm55, singleMM196, original75/561, B-reuse-off154, zero/one prefill56/74,
raw-pair baseline778 and accumulator-pair baseline197. Exact BF16 rounding and
FP32 representability checks pass. Each event's matching k indices contain
one byte-identical complete X vector. The
[eleven-event profile record](evidence/current_20260919/eleven_event_donor_profiles.json)
preserves all candidate counts/ranges and observation limits; none matches
the final 63 K rows. This does not locate the corrupting instruction because
later stale or remapped A delivery remains possible.

This exact fit connects the historical-bit hypothesis to the BF16-visible
arithmetic pattern. It does not establish the faulty instruction or prove
that the enormous training NaNs share this mechanism. A prospective donor
counterfactual will test whether these arithmetic values follow the changed
historical register while effective load addresses remain fixed.

The subsequent matched test waits for the first direct store, explicitly
clears v1, and then uses the later normal read. With verified BF16-one output
prefill, its NOP control fails at **98** with four equal historical-v1 pairs.
The clear arm fails at **87** with nine historical-v1 words in the direct
slots, while **all nine later normal slots contain positive zero**. Every
other output word is zero; no full or half sentinel survives. Full input,
native packet, prefill and first-output checks pass, as do all 150,528 control
and 133,632 candidate pair classifications. The
[complete clear failure audit](evidence/current_20260919/paired_clear_FAIL87_full_failure.json)
and [matched comparison](evidence/current_20260919/paired_clear_comparison.json)
are retained. The explicit write removes the observed corruption between
the two reads. This supports a pre-clear state or delivery problem, while
physical register storage, writeback and read delivery remain alternatives.

The next pair observes v128:v131, the BF16-visible arithmetic family, through
direct and normal reads. Its first control/tagged runs stop at 52/47 on
incidental v1 words before the arithmetic family appears. Both then receive
the same previously reviewed tail C0 reset of v0:v7, leaving v128 arithmetic
and the 2,048-pair mapping unchanged. The matched C0 pair produces the
decisive arithmetic comparison:

| Arm | Retained event | Exhaustive donor/X-row result |
|---|---|---|
| Original historical donor | FAIL34, group 2, source columns 258/322 | 1,085 matching X rows; old donor fits every source and duplicate exactly and under FP32 RNE; tagged donor rejected |
| v257 minus 16, all 12 load offsets compensated | FAIL7, group 1, source columns 130/194 | 960 matching X rows; tagged donor fits every source and duplicate exactly and under FP32 RNE; old donor rejected |

Each event has 288 nonzero physical observations representing 256 distinct
source values, with 32 bit-identical direct/normal pairs and no surviving
prefill sentinel. Every matching row within an event has the same complete
256-feature X vector; the vectors differ between events. The
[matched arithmetic comparison](evidence/current_20260919/C0_donor_arithmetic_comparison.json),
[control full audit](evidence/current_20260919/C0_donor_control_FAIL34_full_failure.json),
[tagged full audit](evidence/current_20260919/C0_donor_tag_FAIL7_full_failure.json)
and independent [control product review](evidence/current_20260919/C0_donor_control_FAIL34_product_review.json)
and [tagged product review](evidence/current_20260919/C0_donor_tag_FAIL7_product_review.json)
all pass. Exact integer checks also account for the rare old-wave-0 FP32
subnormal rounding cases in the saved X domain.

This is a donor-model switch under an address-preserving intervention:
historical register values feed the erroneous arithmetic. The conditional
8/9 factor prediction applies to an identical X profile; these separate
events do not measure that ratio directly. Full audits confirm unchanged
inputs and written outputs. The paired raw reads bypass the BF16 pack and
exclude a fault confined to the later VALU copy or its normal store. Shared
state, read/write delivery or another shared path remain possible physical
fault sites.

[Independent scalar/K-path review](evidence/current_20260919/v513_scalar_K_path_review.json)
finds the suspected low-half v513 read at `0x6c64` executes once, while its
main-loop counterpart at `0x7540` executes 36,314 times. Under the single-lane
B-contamination model and correct staging, compatible saved rows fall on the
repeated path. The [independent A staging review](evidence/current_20260919/mainloop_A_provenance_review.json)
also verifies the repeated A tile and its K=1 fragment membership under
architectural execution.

The completed source-read pair changes only the `0x7540` source0 v513 operand
to inline zero. Its [two-byte candidate review](evidence/current_20260919/v513_source_zero_candidate_review.json)
confirms that destination, permutation selector, source1, all 320 WMMAs,
historical address words, load addresses and paired observations are unchanged.
Both arms retain the final v0:v7 C0 control; v128 accumulation is unchanged.

| Arm | Result | Complete evidence |
|---|---|---|
| Original v513 read | FAIL532 | 256 wrong source values, 288 physical nonzero words, 32 equal direct/normal pairs; only old donors fit all values, exactly and under FP32 RNE, at 552 saved X rows |
| Repeated source0 replaced by inline zero | PASS1000 | All 65,536,000 raw FP32 output words positive zero, all 131,072,000 prefill halfwords BF16 one, all 2,048,000 pairs equal positive zero |

The control's matching rows share one full X profile. Eight rows match the
repeated-site first-phase K sequence; none lies in the final 63 K rows. This
is architectural compatibility under the reviewed staging model, rather than
identification of one transient occurrence. The control's one subnormal
product is exactly representable, so its exact and FP32-RNE row sets agree.
The [full control audit](evidence/current_20260919/source_zero_control_FAIL532_full_failure.json),
[independent product review](evidence/current_20260919/source_zero_control_FAIL532_product_review.json),
[independent passing-stream review](evidence/current_20260919/source_zero_PASS1000_stream_review.json)
and [matched comparison](evidence/current_20260919/source_zero_pair_comparison.json)
are retained. Both native launch joins pass. Final input-byte equality on the
passing arm is producer-reported; there is no full retained final-input artifact
for an independent byte audit.

This bounded clean result supports investigating the repeated v513 consumption
path. Inline zero removes a VGPR read dependency and can change bank routing,
instruction handling or timing. The pair does not prove a permanent fix or
uniquely identify physical register storage, LDS delivery, the permute, WMMA
or the observation path.

The subsequent read-identity pair changes that source from v513 to v514 while
keeping a VGPR operand. Its [independent one-byte review](evidence/current_20260919/v514_reroute_candidate_review.json)
confirms that every other ELF byte is unchanged. Under the historical-bank
hypothesis, the changed source would use v258 donors `0x1010/0x1210/0x1410/0x1610`.
The fitter computes those products from saved X directly; rescaling old rounded
FP32 output words is invalid for six rare old-wave-0 values. The neighboring
source has a different reaching LDS instruction, so equal physical dependency
or timing is not established.

In the first pair, **both the original-read control and v514 arm pass 1,000 calls.** Their
[independent control stream review](evidence/current_20260919/v514_control_PASS1000_stream_review.json)
and [candidate stream review](evidence/current_20260919/v514_candidate_PASS1000_stream_review.json)
verify every output word positive zero, every prefill halfword BF16 one,
and every observed pair equal positive zero. Native joins pass. The
[matched comparison](evidence/current_20260919/v514_reroute_both_PASS1000_comparison.json)
contains no donor event and supports no donor-switch or suppression claim.
The original control ELF is identical to the preceding FAIL532 control;
a 1,000-call pass therefore occurs without a source intervention. This further
limits interpretation of the inline-zero pass.

An exact repetition reuses these binaries, loader, producer and wrapper with
new configuration and output paths. Its **original-read control fails at call
53**, while **v514 passes 1,000 calls**. The control has 256 wrong source values,
288 physical nonzero observations and 32 identical nonzero direct/normal pairs.
All products fit the old v257 donors, exactly and under FP32 RNE, at 620 saved
X rows sharing one full profile. The neighboring-v258 model has no matching
saved X row. Eight rows are compatible with the repeated first-phase K path;
these are conditional candidates, not measured corruption times.

The [repeat comparison](evidence/current_20260919/v514_repeat_FAIL53_PASS1000_comparison.json),
[full control audit](evidence/current_20260919/v514_repeat_control_FAIL53_full_failure.json),
[independent product review](evidence/current_20260919/v514_repeat_control_FAIL53_product_review.json)
and [independent candidate stream review](evidence/current_20260919/v514_repeat_PASS1000_stream_review.json)
bind the completed runs. The candidate again has 65,536,000 positive-zero output
words and 2,048,000 equal positive-zero pairs; both native joins pass. No donor
event occurred in either v514 run. The separate bounded processes establish
neither a failure rate nor suppression, repair or physical fault localization.

The complementary source1 probe keeps source0 v513 at `0x7540` and replaces
only source1 v511 with inline zero. Its
[independent candidate review](evidence/current_20260919/source1_zero_candidate_review.json)
confirms exactly two changed bytes, with all other instructions, bank setters,
historical address values, C0 and paired observations unchanged. Under verified
zero DY, both forms select the same v513 low halfword into the downstream B
operand.

| Arm | Result | Complete numerical finding |
|---|---|---|
| Original source0 v513 / source1 v511 | FAIL123 | Old v257 donors fit all observations exactly and under FP32 RNE at 198 saved X rows |
| Source0 v513 / source1 inline zero | FAIL57 | Old v257 donors fit all observations exactly and under FP32 RNE at 100 saved X rows |

Both failures have 256 wrong true source values, 288 physical nonzero words
and 32 bit-identical nonzero direct/normal pairs, at source columns 258 and
322. Each has three subnormal words, all exact donor products. Complete input,
prefill and native-launch audits pass. The
[matched comparison](evidence/current_20260919/source1_zero_FAIL123_FAIL57_comparison.json),
[control full audit](evidence/current_20260919/source1_zero_control_FAIL123_full_failure.json),
[candidate full audit](evidence/current_20260919/source1_zero_candidate_FAIL57_full_failure.json)
and independent [control](evidence/current_20260919/source1_zero_control_FAIL123_product_review.json)
and [candidate product reviews](evidence/current_20260919/source1_zero_candidate_FAIL57_product_review.json)
are retained.

The historical-donor event therefore persists with the second architectural
VGPR operand encoded as inline zero. That operand is unnecessary for the
observed event class in this setup. The runs use different X profiles and
compatible K sets; this does not identify a physical read, faulty instruction
or common cause with the original training NaNs. Failure calls 123 and 57 are
bounded observations, not a failure-rate comparison.

The completed positive witness uses sparse X and DY at different K positions:
only `X[65,0]` and `DY[66,0]` are BF16 one; all other X/DY words are positive
zero. It retains the existing original and v514 ELFs, packet dimensions and
paired observations. The
[independent numerical review](evidence/current_20260919/sparse_numeric_independent_review.json)
predicts an all-zero original output and one product in true accumulator
`M0,N2 / v128` for v514.

| Sparse arm | Completed calls | Exact raw output in every call |
|---|---|---|
| Original v513 | PASS4 | All 65,536 FP32 words positive zero |
| v514 | PASS4 | FP32 `1.0` at byte offsets 0 and 1024; positive zero everywhere else |

The two nonzero locations are direct and normal observations of the same
accumulator. The [matched comparison](evidence/current_20260919/sparse_v514_PASS4_comparison.json)
joins the loaded ELF identities and native packets to the
[control](evidence/current_20260919/sparse_v513_control_full_audit.json) and
[candidate full audits](evidence/current_20260919/sparse_v514_candidate_full_audit.json).
Both complete retained X/DY/mat2 backings match their prepared values;
all outputs, prefills and final snapshots pass exact byte checks.

The first control native check stopped because its local expected-packet
file was missing. An exact copy of that already-frozen dependency restored
the path; the unchanged checker then passed. The
[independent repair review](evidence/current_20260919/sparse_packet_dependency_repair_review.json)
verifies the copy against the prelaunch source inventory and archive. The
failed check and original frozen files remain preserved.

This is a positive witness that changing the encoded source can produce
the predicted arithmetic change in this kernel. Sparse inputs change the
stress conditions. These eight calls do not identify the physical read or
faulty instruction responsible for historical-donor corruption, establish a
fix or defect rate, or connect the replay error to the training NaNs.

The completed zero-DY probe retains v513 and source1 inline zero while changing
the surrounding source1-bank sequence. Its
[three-arm CPU review](evidence/current_20260919/bank_history_three_arm_CPU_review.json)
compares the existing `2->1->2` sequence, two redundant bank2 setters, and
`2->0->2`. Both modified setters carry consistent previous-mode bytes.
LLVM's author describes those bytes as a firmware requirement; the
[source review](evidence/current_20260919/bank_history_firmware_requirement_review.json)
preserves that explanation and the public ISA's narrower low-byte fields.
The alternatives change transition count or bit pattern and may affect
timing; they do not isolate an unused field from firmware behavior.

| Source1 bank sequence | Completed result | Verified observations |
|---|---|---|
| `2->1->2` baseline | PASS1000 | Every retained output word positive zero |
| `2->2->2`, two redundant setters | PASS1000 | Every retained output word positive zero |
| `2->0->2` | FAIL7 | 256 wrong true sources, 288 physical nonzero words, 32 equal nonzero direct/normal pairs |

The [three-arm comparison](evidence/current_20260919/bank_history_three_arm_comparison.json)
binds all native launches, full prefill/output streams and the
[bank1](evidence/current_20260919/bank1_PASS1000_stream_review.json) and
[bank2 PASS reviews](evidence/current_20260919/bank2_PASS1000_stream_review.json).
The [complete FAIL7 input/output audit](evidence/current_20260919/bank0_FAIL7_full_failure.json)
and [independent product review](evidence/current_20260919/bank0_FAIL7_product_review.json)
find old-v257 exact and FP32-RNE products at source columns 258/322, including
three subnormal values. They match 324 saved X rows; five matches have
`K%64=1`, and none is in the final 63 K rows. The conditional v258 model has
no support in actual X rows.

This failure shows that source1 bank1 at the modified target interval is
unnecessary for the observed donor class. Earlier bank states are unchanged.
The clean baseline and separate bounded processes limit suppression or
failure-rate conclusions about either candidate. Passing runs have verified
producer input-digest records; only FAIL7 retains full inputs for an
independent byte scan.

The installed first-level CWSR handler supplies a specific mechanism now
reproduced by a controlled trap probe. Its [static activation and module binding](evidence/current_20260919/CWSR_installed_static_activation.json)
joins the loaded driver version and packaged GFX1250 shader bytes. In the
non-wave-start entry path, the workaround saves EXEC, selects lane0, then
executes `v_readlane ... v1`, `v_mov v1,0`, a speculative prefetch and
`v_writelane v1,...` before clearing the VGPR bank fields. The
[exact bank/lane review](evidence/current_20260919/CWSR_entry_bank_lane_semantics.json)
checks every one of the 256 bank settings against the installed instruction
words and the ISA.

Under the [independently reviewed published entry contract](evidence/current_20260919/CWSR_published_entry_contract_review.json),
VGPR MSBs persist until an explicit setter or MODE modification. S_TRAP entry
saves the PC and enters privileged mode without listing a MODE reset. Thus
the entry sequence predicts these copies when source and destination banks
differ:

| Setter low-byte example | Predicted lane0 copy |
|---|---|
| `0x01` or `0x05` | Historical v257 into accumulator v1 |
| `0x81` or `0x85` | Historical v257 into B operand v513 |

These are setter encodings, not raw MODE-register bytes. All other lanes
remain untouched by the workaround. The
[executed-window proof](evidence/current_20260919/CWSR_executed_bank_windows.json)
finds seven unchanged `0x81` windows after the v513 LDS load and before its
next repeated consumption, with no intervening v513 write. The
[three-failure geometry review](evidence/current_20260919/CWSR_three_failure_geometry.json)
checks all 864 physical wrong-product equalities across FAIL123, FAIL57 and
the new FAIL7. Lane0 B corruption propagates through the WMMA layout to the
observed lane0/lane16 outputs; four affected waves yield exactly 256 true
sources and 288 physical words.

The [completed sentinel probe](evidence/current_20260919/CWSR_sentinel_full_packet_review.json)
uses the same installed runtime and driver, without a driver change. It
initializes distinct full-word tags in v1, v257 and v513 at lanes 0, 1 and 16,
then compares seven bank settings with and without `s_trap 3`:

| Setter low byte | No-trap result | Explicit-trap result |
|---|---|---|
| `0x00`, `0x41`, `0x45`, `0x82` | All sampled tags unchanged | All sampled tags unchanged |
| `0x05` | All sampled tags unchanged | v1 lane0 changes `0x11000000` to v257's `0x22000000` |
| `0x81`, `0x85` | All sampled tags unchanged | v513 lane0 changes `0x33000000` to v257's `0x22000000` |

Every other sampled tag stays unchanged. Full MODE and EXEC are preserved,
and all seven explicit-trap TTMP0:1 values exactly equal the computed return
PC. All seven no-trap TTMP pairs are zero. TRAP_EN remains set and PRIV is
clear after return. The observed STATUS change `0x400` is VCCZ and is not
used as a trap marker.

The [independent shader review](evidence/current_20260919/CWSR_sentinel_shader_review.json)
checks all 14 instruction bodies, register lifetimes, waits and packet
stores. Each matched pair differs only in the recorded trap flag and the
trap instruction. The [frozen launch manifest](evidence/current_20260919/CWSR_sentinel_launch_manifest.json)
binds 494 files. The [terminal native join](evidence/current_20260919/CWSR_sentinel_native_join.json)
verifies all 94 successful HIP calls and complete retention; independent
packet interpretation scans all 560 output words and 4,480 prefill bytes.

This directly demonstrates the predicted bank copy in the controlled trap
path. The subsequent observer below records a matching return-PC/geometry
observation in a new failing arithmetic replay. Neither result establishes
a common cause with the enormous training NaNs. The live work record is
`L/session_persist_progress_root_v1.md`.

### Completed TTMP observer

The two serial processes completed as **control FAIL43 / observer FAIL332**;
both GPU workers and native verifiers are terminal and reaped. The
[frozen manifest](evidence/current_20260919/TTMP_pair_launch_manifest.json)
binds 1,697 source identities. The observer changes only an 84-byte epilogue
region to read TTMP0:1 and `s_getpc`; all 320 WMMAs and FP arithmetic remain
unchanged. No `s_trap` is injected into this workload.

Exactly **64 duplicate words** become metadata. All 2,048 normal paired
observations and 63,488 unique source observations remain; 1,984 of the 2,048
direct/normal pairs survive. Both arms retain complete 262,144-byte raw
output and prefill images for every call. The control checks all 65,536 raw
words; the observer checks 65,472 arithmetic words and validates metadata
separately.

| Completed arm | Failure | Wrong arithmetic words / unique sources | Source columns / workgroup | Equal nonzero direct/normal pairs |
|---|---|---|---|---|
| `source1_control` | 43 | 288 / 256 | N130/194, group1 | 32 |
| `ttmp_observer` | 332 | 272 / 256 | N258/322, group2 | 16 |

The smaller observer word/pair counts result from removing duplicate slots;
the 256-source corruption geometry is preserved. Both failures are finite,
include two subnormal words, and have no metadata failure. Native packet,
input, full raw stream, and prefill audits pass:
[control native](evidence/current_20260919/TTMP_control_FAIL43_native_join.json),
[observer native](evidence/current_20260919/TTMP_observer_FAIL332_native_join.json),
[control full input/output](evidence/current_20260919/TTMP_control_FAIL43_full_failure.json),
and [observer full input/output](evidence/current_20260919/TTMP_observer_FAIL332_full_failure.json).

**All four affected observer waves sample the same return address.** Their
TTMP64 is `0x00007263877880d4`, while every `s_getpc` reference is
`0x00007263877a0138`. Subtracting the `s_getpc` reference offset `0x20138`
gives module bias `0x726387780000` and relative return PC **`0x80d4`**.
That is the exact instruction boundary for
`ds_load_b64 v[516:517], v283, offset:768`, after setter `0x581` at
`0x80c8` (low bank byte `0x81`). The earlier LDS-load request at `0x80b4` targets
v512:v513, and the later route to permutation `0x7540` and the next WMMA
consumers preserves v513. Under the installed entry-copy mechanism, this is
one of the preselected windows in which historical v257 can enter v513.

The [independent complete PC-context audit](evidence/current_20260919/TTMP_observer_PC_context.json)
checks **all 5,312 wave samples** and the exact instruction bytes. The only
samples in any of the **14 preselected heads across seven bank81 windows**
are the failing call's four group2 waves, exactly the complete wrong-source
wave set. None of the preceding 331 calls samples these heads. All getPC
references and module biases are valid and constant throughout the run.

Of the 332 calls, 53 (52 passing plus the failure) have all 16 sampled TTMP
values matching current-module instruction boundaries. The other 279 calls
have nonzero TTMP values with no exact current-module boundary match. There
are no zero TTMP samples. **This does not turn the snapshots into a complete
trap history:** initial TTMP is not guaranteed zero, the snapshot carries no
age/loop counter, and a later event can hide an earlier one. Handler restart
adjustments and asynchronous LDS completion also limit assignment of the
first-level handler's entry state. `0x80d4` is a sampled resume boundary;
its LDS instruction is not established as the instruction that corrupts v513.

The [control donor fit](evidence/current_20260919/TTMP_control_FAIL43_products.json)
finds 19,087 matching saved X rows; the
[observer donor fit](evidence/current_20260919/TTMP_observer_FAIL332_products.json)
finds 368. In both, the historical v257 model fits every wrong source and
surviving duplicate under exact and FP32-RNE arithmetic; the neighboring
v258 model is excluded (zero exact-search matches; FP32-RNE product-domain
constraints reject it before a full-X search). The entire saved X backing is rehashed.
Multiple matches prevent identifying a unique K row or event time. An
[independent complete product verification](evidence/current_20260919/TTMP_both_failures_independent_products.json)
reproduces both complete match lists while independently decoding all wrong
coordinates and rehashing both full output streams and the entire X backing.

The first frozen donor-fitter invocation stopped on a JSON identity-schema
comparison: the full-input auditor also records
`stable_during_wholefile_read: true`. A separately
[reviewed post-run adapter](evidence/current_20260919/TTMP_donor_identity_adapter_review.json)
requires that true annotation and removes only it from four in-memory
identity records. All path/size/hash values, frozen numerical/search code,
raw-data checks and original files remain intact; each fit records the
adapter provenance. The failed invocation is retained. An
[independent adapter/result join](evidence/current_20260919/TTMP_adapter_result_joins.json)
verifies that only those four annotations changed in memory and rejoins all
wrong words, surviving pairs, model inputs and sampled wave PCs to the
original retained files.

The [root comparison](evidence/current_20260919/TTMP_pair_result.json)
independently rereads the complete observer stream and rebuilds the
coordinate/PC association. Together with the controlled sentinel and donor
interventions, this strongly supports the installed trap-entry bank-copy
mechanism for the replay. It remains a same-invocation association pending
a kernel counterfactual; it does not establish a shared mechanism for the
untraced enormous attempt-405 training result.

The September21 **before/after TTMP probe** now resolves snapshot freshness
for a separate bounded scalar wait: 160 calls complete with all packet,
native, prefill, input-argument, control and library checks passing, including
80 explicit tag-plus-PC calibrations. Four no-injection calls have changed
masked TTMP PCs inside their own wait loop, with unchanged tags: three under
bank00 and one under bank86. The 40 vulnerable-bank no-injection calls have
no fresh observed event. This demonstrates fresh equal-bank exposure in
these invocations, while spontaneous vulnerable-bank exposure remains
unobserved in this sweep. The tag snapshot window is wider than the TTMP
window; co-occurrence would not identify one uniquely causal trap.
[Complete 160-case audit](evidence/current_20260919/fresh_TTMP_160_terminal.json).

The completed reversed-order sweep uses the same shader and criteria for
800 calls. Its 400 no-injection calls include three predicted copies with
fresh in-loop PCs: bank05 at ordinals19/379 and bank46 at ordinal159.
Equal-bank ordinals25/37 have fresh PCs with unchanged tags. The remaining
395 no-injection calls have no fresh observed in-loop PC; all 400 explicit
calibrations pass. The independent terminal audit checks 2,400 retained
images and 4,810 HIP calls, with zero failed checks. A separate restoration
audit rereads all 460,800 image bytes and independently reconstructs the
tag/freshness counts. This supplies spontaneous vulnerable-bank exposure
evidence for the probe, with the same snapshot-window and training limits.
[Restored 800-case result](evidence/current_20260919/fresh_TTMP_800_restored.json).

The later NOP-only process invokes exactly 400 `_0` entrypoints and no
calibration entrypoint. It records bank05 copies at ordinals25/37/309 and
bank46 copies at ordinals19/159/199, each with a fresh current-plateau PC.
Six equal-bank calls have fresh PCs and unchanged tags. The independent
terminal audit and root's full-image/tag/PC reconstruction agree; all calls
are retained. This stronger no-injection control leaves the source and
complete count of spontaneous traps unrecorded.
[NOP-only evidence](evidence/current_20260919/fresh_TTMP_NOP_only_result.json).

### Completed equal-bank PC comparison

The independent complete audit now covers both the bank81 **FAIL203** control
and bank82 **PASS1000** candidate. It rechecks every retained output and prefill,
all native A/B/C/D pointer joins, raw metadata, source-wave mapping and actual
ELF instruction bytes. The candidate has **65,472,000 positive-zero arithmetic
words**, valid metadata in all **16,000** wave snapshots, **10,672** exact
current-module instruction-head matches and **5,328** nonmatching nonzero
TTMP values. The `s_getpc` reference and inferred module base are constant.

None of its snapshots matches the original 14 selected LDS instruction heads.
Separately, **84** snapshots at exit setter `0x813c` and **96** at `0x815c`
occur in **45** passing calls. Both setters immediately follow an equal-bank
window. These adjacent locations were examined after the run and are kept
distinct from the preselected criterion; a saved resume PC alone does not
measure the handler's entry bank state. The control recheck reproduces the
four `0x8114` samples in exactly its four affected group1 waves.

The candidate also retains the control failure's exact absolute TTMP value,
`0x7088d9988114`, in group1's four waves on **11** later calls (**44** samples).
Its current module base is `0x7b8910780000`; that old absolute PC lies outside
every current ELF load segment. These observations therefore do not count as
candidate exposure at `0x8114`. Only 97 distinct complete 16-wave TTMP vectors
appear across its 1,000 calls. Repeated snapshots cannot be counted as
independent trap events. The [complete comparison](evidence/current_20260919/BANK_EQUAL_complete_PC_comparison.json)
and [age/ordering addendum](evidence/current_20260919/BANK_EQUAL_snapshot_age_and_entry_ordering.json)
retain the raw-stream identities and full external audit paths.

The published S_TRAP execution model drains outstanding instructions before
entering the handler. Under that model, the preceding LDS write completes
before the handler's early register copy. No self-copy/LDS race has been
observed here; such a hypothesis would require behavior outside that documented
ordering. This qualifies generic asynchronous-LDS caveats in older certificates.
The passing candidate supplies bounded supporting evidence, with no sampled
match to the original 14 heads, and establishes neither a training fix nor
a failure rate.

### Completed actual-DW gated trap matrix

The reviewed single-trap candidates inject exactly one `s_trap 3` per wave at
the controlled DW location, preserving the ordinary arithmetic and observation
layout except for the intended trap instruction. The NOP family checks the
same two bank layouts without the injected trap. All four arms use the frozen
runtime, owned ELF/native-launch joins, complete prefill streams and retained
raw FP32 arithmetic images:

| Arm | Result | Forced return-PC samples | Arithmetic oracle |
|---|---|---|---|
| NOP bank81 | PASS4 | 0 / 64 waves | All retained arithmetic words match zero oracle |
| NOP bank82 equal-bank | PASS4 | 0 / 64 waves | All retained arithmetic words match zero oracle |
| Trap bank81 | FAIL1 | 16 / 16 waves at `0x4468c` | **1,088 nonzero words / 1,024 sources / 64 equal direct-normal pairs**, exactly matching the predicted corruption image |
| Trap bank82 equal-bank | PASS4 | 64 / 64 waves at `0x4468c` | All retained arithmetic words match zero oracle |

The [matrix summary](evidence/current_20260919/gated_actual_DW_trap_matrix.json)
pins the local source reports, native joins and oracle-audit hashes. This is
the strongest actual-kernel counterfactual so far for the installed CWSR
bank-copy mechanism: when the injected trap returns under the hazardous bank81
configuration, the predicted donor arithmetic appears; when the same controlled
trap executes under equal-bank routing, it does not. The result remains an
injected-trap experiment, not proof that the spontaneous historical replay or
training failures trapped at the same time, and the PASS4 arms are bounded
nonreproductions rather than stability estimates.

## Full versus shrunk model

Both full-model host reports were reviewed: [a37-1](../mi450_1P4G/ctheliosp-1b112-a37-1.md)
completed one 3,000-step finite-loss run, and
[a37-2](../mi450_1P4G/ctheliosr-1b112-a37-2.md) subsequently completed
**14,000 steps in 9h 9m 37.875s**, with all 14,000 logged losses finite and
holdout **AUC 0.7514703274**. Its trainer/controller exited successfully.
Two earlier 3,000-step runs are separate trajectories. The long run uses
uncapped attention and all **293,051,712 embedding rows** across four GPUs,
with local/global batch 1024/4096, base LR 5e-7 and 24,000-step warmup.
It is substantial finite-loss and convergence evidence, with a different
observation boundary from A0's saved raw gradients. The latest reports were
pulled at `6950f7f`; the successful training used `191adbc`.

The A0 reduction is **embedding vocabulary scaling**, not reduced dense
depth or width. Both setups retain three HSTU layers, width 512, four heads,
sequence limit 4096 and local batch 1024. Quarter scaling has **73,262,927
rows**. Source and installed TorchRec inspection establish these effects:

- Base IDs wrap modulo the smaller vocabulary and cross-feature hashes use
  smaller capacities, changing collisions and sparse update frequency.
- Fused TorchRec actually initializes shards with bounds
  `±sqrt(1/num_embeddings)`. Quarter rows therefore give approximately
  **2× initial amplitude / 4× variance**. This is confirmed in the installed
  fused initialization path, not inferred only from the eager `init_fn`.
- Allocation size, shard placement, random-number consumption and sparse
  execution can also change. Row scaling itself does not change history
  selection or labels, so dense shapes should match at fixed batching.

The selected preprocessing path is artist/album embeddings `[K,1024]` →
Linear256 → SwishLayerNorm → captured X `[K,256]` → Linear512 → LayerNorm.
Here SwishLayerNorm computes `Z * sigmoid(LayerNorm(Z))`; its multiplicative
Z branch means normalization does not remove amplitude sensitivity.
K counts history and candidate rows before the eight context tokens are
added. Base-ID modulo and raw-anchor cross hashes change feature identities;
UID, item and cross-feature changes also affect the downstream DY trajectory.
This source-level path explains routes to changed bits, without predicting
the successful run's actual operands. Dense parameters initialize before
the later fused-embedding reseed, so that reseed alone is not evidence of
changed dense initial parameters.
[Source and installed-initializer review](evidence/current_20260919/1P4G_model_dataflow.json).

The sampler assigns global indices by rank before forming local batches.
Four GPUs therefore **do not imply K/4 for this dense DW**: each rank still
processes 1,024 samples, with different anchors/windows. A0's captured K is
2,324,351; the successful run's actual per-rank K, DW binary and workspace
selection were not retained in the report. **All 250 files in A0's frozen
attempt-405 source archive match the successful run's recorded commit
`191adbc` byte for byte**, including the two gin files and all 248 source
entries. They also match current `6950f7f`. A change to those committed files
does not explain the different outcome; actual remote installed libraries
and execution state remain unverified.
[Complete archive comparison](evidence/current_20260919/1P4G_complete_A0_source_overlap.json),
[detailed comparison record](evidence/current_20260919/1P4G_comparison_evidence_manifest.json).

| Recorded condition | A0 attempt 405 / current replay | Successful a37-2 run |
|---|---|---|
| Embedding vocabulary / ranks | Quarter vocabulary / one rank | Full vocabulary / four ranks |
| Local / global batch | 1024 / 1024 | 1024 / 4096 |
| Allocator | Captured `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` | Both allocator aliases explicitly cleared |
| Attention / weighted-LN override | cap256 / BN1 | Uncapped / override0, using existing selection policy |
| Kernel / AMDGPU | 6.14.0-37 / 7.1.1.31300009 | Custom 6.16.1 / 7.1.0.31300009 |
| Current recorded firmware | 630A, July25 | 640C, August19 |
| Selected CWSR handler bytes | Installed 5,656-byte image bound and controlled copy reproduced | Not recorded; version strings do not establish identical or corrected code |
| Native selected DW | Replay has verified 676-VGPR kernel and exact packet | Full-run native binary/packet unrecorded |

The successful post-AC driver load explicitly used `noretry=0`,
`gpu_recovery=0` and `ip_block_mask=0xcff`. Under the local gfx12.1 source,
A0's current `noretry=-1` resolves to retry disabled; explicit0 enables
retry and changes XCC and KFD queue fields. This can affect page-fault
handling and execution exposure, but does not preserve VGPR bank contents.
An in-place parameter write would not attest the initialized GMC/KFD state.
Under A0's recorded IP registration order, mask0xcff disables VCN/JPEG and
keeps compute, SDMA, MES and UALink enabled. Recovery0 controls hang/reset
paths. The 9000ms classic-HWS wait timeout and MES's submitted 10ms/1ms
process/gang quantum values are not measured trap rates. Actual a37-2
source, registration order, effective queue controls and CWSR state remain
unavailable; these are conditional source interpretations, and A0's current
snapshot does not attest attempt405's historical state.
[Scheduler and retry source evidence](evidence/current_20260919/1P4G_scheduler_semantics.json).

Neither `gfx1250` nor PCI revision `0x00` establishes the successful boards'
A0/B0 stepping. The installed A0 workaround comes from a private driver
branch; the retained public ROCm 7.1.0 and 7.1.1 sources both lack that block.
Comparing public version labels therefore cannot establish the successful
host's actual handler. The latest B0 report also combines a controlled
trap-caused LayerNorm NaN with a separate 3,000-step finite training pass,
showing that a long finite trajectory can coexist with a live defect.
Its September21 continuation reports the same 5,656-byte selected CWSR
image as A0 despite a different driver package build ID. B0 also reports
all 402 hipBLASLt catalog files equal to A0, yet a projection-DX control
selects a different tile at the same shape. These are B0 observations,
not actual handler or DW-selection evidence from the successful a37 host.
They strengthen the need to compare executed bytes and launch arguments.
[Additional comparison evidence](evidence/current_20260919/1P4G_additional_discriminators.json).

The existing cross-host comparison also changes global batch 1024→4096,
sample/rank assignment and eligible windows, sharding and communication,
allocator settings, driver/kernel/firmware, and observation. Both 1P4G
launchers clear the expandable-segment allocator setting used by A0.
Equal seed and optimizer step do not imply equal matrices or states.
The resumed A0 boot reports VBIOS **`113-M4500001-630A`**, build `00195379`,
dated **2026/07/25**. Both 1P4G reports use **640C**, dated **2026/08/19**;
B0 reports **650D**. These are additional recorded differences, not a firmware
cause or fix claim (`L/session_resume_current_VBIOS_v1.json`).

Scaling is thus a plausible **trigger**, but ordinary collision/init-induced
optimization instability does not explain the captured attempt-405 DW:
its small finite saved inputs cannot mathematically produce `1e31` or NaNs.
The isolated zero-DY DW failure executes without embedding tables or an
optimizer. In the now-tested address-copy mechanism, packed DY bits become
an address: changed values can alter bounds, byte alignment and the resulting
corruption. This gives model-dependent trigger exposure a concrete mechanism
without assigning the cross-host difference to it.

A conditional CPU byte analysis makes this sensitivity explicit. Changing
only the low donor BF16 from `0xb101` to adjacent `0xb100` changes its value
from −1.877197e−9 to −1.862645e−9, and the copied offset from odd
`0x3176b101` to even `0x3176b100`. Both address streams have 1,576 in-range
loads. The original stream contains 53 NaN **input payload words** and finite
values up to 2.472364e38; the even stream has no nonfinite payload and maximum
0.002700806. These input counts differ from the known 640 output NaNs.
Flipping the high donor's sign instead makes every modeled load out of bounds.
This initial donor-bit sensitivity calculation preceded the modified-input
GPU experiment below; it does not simulate the full model. The independent
review rechecks complete original X/DY hashes and all selected payload bytes.
[Byte analysis](evidence/current_20260919/training405_operand_bit_sensitivity.json),
[independent review](evidence/current_20260919/training405_operand_bit_sensitivity_independent.json).

The completed **one-bit GPU test confirms finite corruption** under the same
handler and unchanged native NOP/TRAP binaries. The original capture remains
unchanged; a CPU clone changes one byte at offset617022852, `01→00`, a scalar
delta of exactly `+2^-36`. All four NOP calls still match the complete original
baseline. All four trapped calls have exactly the same 1,001 finite changes
inside the predicted 1,024-word block, no NaNs/Infs and no outside changes.
The maximum target magnitude is `1.2099742889404297e-5`. All eight target
outputs fit the prelaunch FP32 reduction-tree intervals, and all 204 cells
whose NOP/TRAP intervals were disjoint actually differ. The narrower central
model matches only 1011/1024 NOP and 1012/1024 TRAP words; the rounding
discrepancies remain unassigned. Thus different model-produced bit patterns
can change numerical damage dramatically while the defect remains present.
The successful run's finite losses and AUC do not establish whether it avoided
the defect, encountered small finite errors, or used a different handler/kernel.
[Hardware result](evidence/current_20260919/training405_onebit_DY_result.json),
[formal matrix](evidence/current_20260919/training405_onebit_DY_matrix.json),
[interval diagnostic](evidence/current_20260919/training405_onebit_interval_comparison.json).

At fixed input bytes, shape, storage origin and descriptor controls, moving
the complete X allocation alone cancels from the relative address formula.
Allocator settings can still change selection, synchronization, lifetimes
and trap timing; simple relocation alone does not change this reconstructed
payload. Actual 1P4G X/DY and descriptor records are needed to test exposure.

An unrun cross-host discriminator would compare the actual loaded CWSR
image, scheduler policy and native runtime, followed by the same controlled
original-input DW and the successful training's native DW dimensions/binary.
These comparisons would distinguish a different handler
from the same defect under different inputs or execution history before a
longer row-scale comparison. Access to the successful host's raw module and
run artifacts is still unresolved in this continuation; documentation alone
does not supply those bytes.

The discriminating training comparison is **one healthy four-GPU host**,
fixed four ranks/local 1024/global 4096, fresh scale **1.0 → 0.25 → 1.0** arms,
each bounded at first failure or 3,000 steps. Preserve its successful source,
stack, allocator, transport, kernel controls and data batching. Record actual
TBE shard/rank assignments and selected CPU anchor/length hashes; automatic
placement can change with row scale. A quarter-only failure would justify
separating initialization bounds, ID collisions and memory footprint in
follow-up controls. Full tables alone require 558.95 GiB and cannot fit A0's
single 432 GiB GPU, so changing A0 to UVM or another batch would confound that test.

The complementary short comparison is the exact saved zero-DY DW operation
on a successful 1P4G host, matching the failing native binary, symbol,
orientation, shape and arguments, with solution102 as a control. This tests
the component without the training model. **Neither remote comparison has
been run in this session.** Detailed code references and installed-source
hashes are in `L/session_resume_full_vs_shrunk_agent_v1.md`.

## Completed work and open questions at stop

<a id="next-root-cause-steps"></a>

The user explicitly stopped the investigation. The following records the
completed work and remaining gaps; no follow-up is active or scheduled.

1. **Corrected-handler activation remains unresolved.** The
   actual-DW gated trap matrix is complete: NOP bank81 and bank82 pass four
   calls each, injected-trap bank81 fails on call1 with the exact predicted
   corruption image, and injected-trap bank82 passes four calls while recording
   the expected forced return PC in every wave. The bank-neutral CWSR
   correction's complete handler and scratch driver build
   now pass byte, branch, CPU-code, and relocation audits. The prepared
   original/candidate/restored sentinel comparison was interrupted during the
   driver switch, before a candidate result. The original driver and sentinel
   have since been restored and verified on a new boot. The activation route
   remains unresolved; the old controller's output paths are already used.
   No corrected-driver hardware result or training fix is established yet.
2. **Complete DW coverage audit and controlled relocation validation passed.**
   The combined seven-event gate now returns all 131,072 historical words
   exactly in four trapped calls, including the six v1/lane0 residuals;
   joint equal-bank prevention returns the full normal baseline in all
   eight NOP/trap calls. Both frozen matrices pass. The completed single-bit
   DY treatment retains the same single-target trap and yields 1,001 small
   finite errors in every trapped call, with all frozen criteria passing.
   The completed static inventory binds 46,658 instructions and 11,134
   bank setters, including 5,486 unequal source0/destination intervals in
   all six copy directions. Many are generic epilogue paths; these counts
   are not dynamic exposure counts. The relocation inventory identifies
   13 connected registers and 1,675 decoded operand instructions, including
   the two initial ABI reads retained by the final candidate. The initial
   count 1,699 included 24 false positives from half-register
   annotations; its report is retained with the correction. All 1,675
   isolated original instructions reassemble byte-exactly, and their remapped
   forms encode/decode without conflicting bank-field requirements.
   [Corrected coverage and feasibility](evidence/current_20260919/DW_full_bank_mitigation_feasibility_v2.json),
   [parser correction](evidence/current_20260919/DW_sink_inventory_erratum.json),
   [instruction encoding review](evidence/current_20260919/DW_sink_instruction_encoding.json).

   Complete matched control/candidate ELFs and their combined-trap variants
   are now built. The candidate relocates 13 connected registers, preserving
   the two initial ABI reads, and leaves physical v1/v257/v513 unused by the
   program. All 320 WMMAs remain present. Both arms share 1,456 inserted
   bank-setting brackets and the same 704-register allocation; the candidate
   changes 1,673 original operand instructions. Contextual control-flow,
   lane-definition, scheduling, sink-use and final ELF-layout reviews pass.
   The arithmetic prerequisite now passes: all four complete saved-input
   outputs in each arm equal the frozen ordinary baseline, with native,
   complete-input and runtime audits passing. The seven-event quartet also
   passes: four complete historical outputs in the trapped control and four
   ordinary outputs in each of the other three arms. These binaries remain
   investigation candidates; natural failure suppression and training repair
   have not been validated.
   [Lane-definition review](evidence/current_20260919/DW_sink_full_lane_definitions.json),
   [unused-register review](evidence/current_20260919/DW_sink_noninterference.json),
   [final ELF review](evidence/current_20260919/DW_sink_final_ELF_layout.json).

   The proposed **1,000-call-per-arm natural zero-DY comparison was not run**.
   Preparation stopped with derived producer/wrapper and loader sources,
   partial auditors and a passing design review. The package has no final
   launch manifest or release, and the terminal auditor is explicitly not
   ready to freeze. Its proposed oracle accepts either signed BF16 zero;
   all nonzero and nonfinite words fail. No comparison outcome is available.
   Files remain under `L/session_20260921_restore_sink_zero_package_v1`,
   `..._zero_audit_v1`, `..._zero_peer_v1` and
   `D/session_20260921_restore_sink_zero_runtime_v1`, inventoried in the
   [stopped checkpoint](evidence/current_20260919/session_stop_20260921.json).
3. **SVM/queue tracing remains CPU preparation only.** The fresh-process
   NOP-only run already establishes six spontaneous predicted copies without
   same-process explicit calibration. Their trigger source is still unknown;
   **SVM eviction/restore is a hypothesis**, with no queue/trap correlation
   result. Static review found that the seven existing KFD tracepoints omit
   the direct SVM path. A separate package adds two scoped helper kprobes,
   installed-module/BTF argument bindings, per-call BOOTTIME/MONOTONIC
   timestamps, process-identity handshake and guarded cleanup. Its CPU
   preflights and independent review pass, but **the probe was never activated
   or run**. Helper entries would record attempts, not acknowledgments of a
   hardware context save. The package is
   `L/session_20260921_fresh_TTMP_ftrace_package_v1`; its retained
   `READY_FOR_ROOT_LAUNCH` release predates and is superseded by the user stop.
   The final snapshot has no trace instances or kprobes; the existing global
   `tracing_on=1` setting was left unchanged.
   [Package inventory and CPU-review identities](evidence/current_20260919/session_stop_20260921.json).
4. **Driver, model and parallelism differences remain unisolated.** The
   successful 1P4G trajectory reaches 14,000 steps and holdout AUC0.75147.
   Its actual CWSR image/policy, native DW identity and exact saved-input
   component comparison remain unavailable; neither remote comparison ran.
   All 250 A0 archived source files match its recorded source commit; the
   handler, allocator, inputs, sharding and execution history remain distinct.
5. **The combined training result remains bounded.** The
   1,000-step run and full final-frame audit are complete. Longer independent
   runs remain necessary for a fix claim, with matching tracing, catalog and
   capture policy across arms.

## Stack and controls

<a id="21-pins"></a><a id="81-hardware-and-recovery"></a>

Latest recorded pull: **`6950f7f`** on
`chcai/mi450`. The completed combined capture used source base `a7c1e8b`;
its 38 frozen launch/source checks pass. Archived hashes identify the exact
diagnostic source used at each capture.

At the September19 resume the host had rebooted at **18:24 UTC**, after
the completed training run. `/dev/kfd` was absent because amdgpu was not
loaded. The archived A0 boot recipe explicitly calls for plain
`sudo modprobe amdgpu`; that completed successfully at **20:03:28 UTC**.
The original container was restarted and the recorded Torch/HIP/Triton stack,
one visible GPU, 256 CUs and 432 GiB HBM were verified. Current default
`gpu_recovery=-1` is recorded; no reset, reboot or KFD `hqds` read was issued.

At the September21 continuation, the host had rebooted again and amdgpu was
unloaded. The same documented module-load command and original container
restore succeeded. The container/image identity, driver version, `630A` VBIOS,
single gfx1250 device, 256 CUs and 432 GiB HBM match the retained configuration.
A new CPU-oracle gather/index-select health probe passed 1,000 calls for each
of four mode/width combinations, with complete source/index/output checks.
The [runtime restoration record](evidence/current_20260919/runtime_restoration_20260921.json)
pins this new boot and driver bytes. No reset or reboot was performed by this
continuation; the new boot is a difference from the September20 comparison.

A further reboot at approximately **02:59:15 UTC on September21** interrupted
the scratch-driver comparison. Its cause remains unknown. The unchanged
installed module was restored at **03:04:25 UTC**, followed by the original
container and matching Torch/HIP/Triton, VBIOS and device properties. Four fresh
CPU-oracle data-movement arms each passed 1,000 calls. A complete 14-case
sentinel then reproduced the original driver's three expected bank-copy cases,
with all control tags, MODE/EXEC and seven forced return PCs verified. The
[recovery record](evidence/current_20260919/CWSR_driver_interruption_recovery.json)
binds these new-boot results and recovered pre-switch evidence. No reset or
reboot was performed by the recovery continuation.

A fresh CPU-oracle health probe passed **1,000 calls each** for `gather` and
`index_select` at widths 16 and 4. Source and indices were created on CPU;
every full output and index readback was checked on CPU, and full source
readbacks matched before/after each arm. This bounded pass does not establish
WMMA correctness or clear intermittent component defects. The runtime/health
evidence is pinned in the resume summary; raw report is
`D/session_resume_gather_health_v1.json`.

| Item | Current recorded configuration |
|---|---|
| Host / target | `heliosr-1b114-c07-1`, A0 single GPU, `gfx1250`, approximately 432 GiB HBM; Linux `6.14.0-37-generic`, amdgpu DKMS 7.1.1 |
| Current boot VBIOS | `113-M4500001-630A`, build `00195379`, version `025.040.000.009.000001`, date `2026/07/25`; read from sysfs and matching boot log |
| Image / container | `recommendation-gfx1250-20260910:triton-7ff97e` / `triton-7ff97e-20260910` |
| Torch / HIP | `2.11.0+rocm7.14.0a20260625` / `7.14.60850` |
| Triton | Upstream `7ff97e310935b4a79794878dbc911f9af25d38d9`, reports 3.8.0; built into the image |
| Completed combined training | Seed1, start timestamp0, batch1024, one GPU, embedding-row scale0.25, expandable segments, no evaluation/checkpointing |
| Required execution controls | `AMDGCN_USE_BUFFER_OPS=0`, `TRITON_FULL_AUTOTUNE=0`, `TRITON_ALLOW_PIPELINING=0`; completed combined run uses cap256/BN1/workspace1/private catalog as above |

Set execution variables **before importing** the model/kernel modules.
`gfx1250` does not select A0 versus B0 policy. The current HSTU cap default is
0; older B0 code defaulted to 256, so specify the intended value explicitly.
Use matching capture/replayer/source revisions and preserve source guards;
do not disable a guard to make a historical payload load.

Source/code changes or another GPU workload during a run invalidate its
comparison. Preserve the running process and its external files. A fault or
stale progress needs inspection of the
actual worker/trace before classifying failure; do not infer completion from
`outcome.json`'s initial counters or a surviving shell alone.

## Historical porting changes and rollback record

<a id="4-changes-applied-to-master"></a>

This section retains the **September 1–11 bring-up record from `8377ae9`**
against **`mlcommons/training@b8b3adc`**, the branch baseline. The original
reference logs in [`rcp_logs/`](../../rcp_logs) identify `submission_platform:
MI350`; that recorded name is retained here for the reference implementation
referred to as MI355 in the discussion.

**Each row preserves what changed, where, why, its measured or possible cost,
and what would justify removal.** Row numbers and bracketed defect IDs match
the original edition. File locations and implementation counts describe that
historical revision; check the current source before reverting. The current
[stack controls](#stack-and-controls) and [NaN status](#current-nan-status)
take precedence over old gate statuses or run instructions.

Rows 1–4 are execution workarounds; row 5 is an API port; rows 6–8 make the
fbgemm build usable; row 9 changes the model; rows 10–12 are metrics and
diagnostics; row 14 is language compatibility. **Rows 13 and 15 are source
correctness fixes and should be retained**, even if the configuration gates
that exposed them are removed. In particular, the int64 widening in row 15
is separate from Triton's removal of block pointers.

| # | Change (where) | Motivation | Perf impact | Potential downside | Potential follow-up |
|---|---|---| --- |---|---|
| 1 | **`AMDGCN_USE_BUFFER_OPS=0`** — env, defaulted by [`scripts/run_smoke_shrink_1gpu.sh`](../../scripts/run_smoke_shrink_1gpu.sh) | [1]: the buffer-op pass group wedges the **whole node** on kernels whose pointer args straddle the 2 GiB narrowing cutoff, so one body mixes 32-bit `buffer_*` and 64-bit `global_*` addressing. [2]: the same group silently corrupts 20 / 7640 elements in the position kernel | **Unmeasured, and may be unmeasurable here.** An A/B needs buffer ops *on*, which hangs the node ([1]) | Disables `canonicalize_pointers` → `convert_to_buffer_ops` → `optimize_buffer_op_ptr` for every kernel; the perf cost is unmeasured. Does **not** cover [1b], where hangs still occur with the knob off | Triton fix for hybrid 32/64-bit narrowing on gfx1250, and CI above the 2 GiB cutoff. Owed locally: a matched [1b] control, knob on vs off, one test per process |
| 2 | **`BLOCK_N=64`** for the pinned attention-backward config — `_get_bw_pinned_configs()` in `ops/triton/triton_hstu_attention.py`, gated on gfx1250 + Triton ≥ 3.8 ([PR #7](https://github.com/chriscai-amd/training/pull/7)) | [6] / [4d]: `_hstu_attn_bwd` stores to a wild address mid-run. The historical isolation identified an address-critical value held in an **extended VGPR across the WMMA region** ([#2](https://github.com/raikonenfnu/training/issues/2), [#3](https://github.com/raikonenfnu/training/issues/3)); the N=128 tile is what exposes it | **Measured: 1.54× kernel slowdown.** 11.22 ms at `BLOCK_N=64` vs 7.30 ms at 128, ±0.05 ms, at 654 vs 987 VGPRs with no spills. e2e A/B impossible — 128 faults. The largest confirmed regression in this table | Halves the N tile; end-to-end cost unmeasured (kernel cost measured at left). The hazard itself is untouched, so another shape that produces the same allocation can still hit it, and `TRITON_FULL_AUTOTUNE=1` restores the N=128 config. The gate calls `torch.cuda.get_device_properties(0)` at **import time**, initialising a HIP context on device 0 before a spawned rank selects its own | Land the LLVM-side fix ([#3](https://github.com/raikonenfnu/training/issues/3): rematerialise address-critical work-item values after WMMA regions, gated as a gfx1250-A0 erratum), then drop the pin. Run the cold-boot **native B0** matrix to decide whether this is A0 silicon or a backend requirement. Measure the N=64 vs N=128 throughput delta |
| 3 | **`clamp_num_stages`** forcing `num_stages=1` — `common.py`, gated on HIP + Triton ≥ 3.8, overridable with `TRITON_ALLOW_PIPELINING`. It sits in the OSS `triton_autotune` wrapper, so it reaches the 30 kernels that route through it across eight `ops/triton` files | [4]: 3.8.0 miscompiles software-pipelined loops. With `num_stages ≥ 2` attention faults at `gstep=0` or returns 3.3e+28 garbage; at `1` it is bit-comparable to 3.6.0. Not attention-specific — the jagged and layer-norm kernels autotune to `num_stages=3` and fault too | **Measured 2026-09-11: none — slightly _positive_.** 3397/3403 ms clamped against 3492 ms unclamped at batch 1024, so the clamp is **2.8 % faster**. The earlier "likely material" cost is withdrawn | Loses pipelining across all of those kernels. **Cost measured 2026-09-11 at batch 1024 and it is negative: the clamp is 2.8 % _faster_** (3397/3403 ms clamped against 3492 ms unclamped), so the earlier "likely material" cost is withdrawn. Inert on 3.6.0 / 3.7.1, which keep their original staging. **Coverage is not total:** `_weighted_rms_norm_fwd` (`triton_layer_norm.py:785`) uses a raw `@triton.autotune` and is never clamped — it has not faulted, but it is not protected either | **Re-checked 2026-09-11 at batch 1024: [4] did not reproduce** — 600 steps unclamped, `EXIT=0`, finite ([historical §5.1](history_8377ae9.md#51-re-test-queue), rank 7). The `nan`-at-step-100 evidence behind the 2026-09-04 re-check was **batch 8, now retired** ([9.5](history_8377ae9.md#95-the-batch-8-pipelining-nan-4-pre-fix-stack)). The gate stays on the narrower ground that removing it is a measured regression for no observed benefit, not that [4] is proven live. Owed: a plain-pointer rewrite of [`repro_gfx1250_pipeliner.py`](../../scripts/repro_gfx1250_pipeliner.py) — it cannot compile on current main ([5]) — aimed at a jagged or layer-norm kernel |
| 4 | **Separated-RNG dropout path forced on** — `_fused_rng_ln_mul_dropout_is_broken()` in `ops/utils.py`, folded into `use_separated_rng_ln_mul_dropout()`, gated on gfx1250 + Triton ≥ 3.8 | [4c]: `_ln_mul_dropout_bwd_dx_du` dies with `HSA_STATUS_ERROR_MEMORY_APERTURE_VIOLATION` whenever dropout is active. Proximate cause is the gfx1250 coexec machine schedule under LLVM expert mode ([#1](https://github.com/raikonenfnu/training/issues/1)) | **Unmeasured.** Splits one fused kernel into separate RNG and LN/dropout work, so extra launches and traffic are plausible but unquantified. The removal candidate with the clearest theoretical upside | Low risk — it is an upstream-supported path that sm100–103 and gfx950 already take — but whether gfx1250 wants it on **performance** grounds was never measured, and settling that on a miscompiling version would be meaningless | [#1](https://github.com/raikonenfnu/training/issues/1)'s retest reports latest main passing the original two- and four-warp launches, so re-check on `7ff97e3109` and narrow the gate to AMD 3.8.0. Then decide the separated path on perf alone |
| 5 | **Pointer port off `tl.make_block_ptr` / `tl.advance`** — 26 + 8 call sites across `ops/triton/triton_hstu_attention.py`, `triton_layer_norm.py`, `triton_hstu_linear.py`. See [API changes](#63-triton-language-api-port-5) | [5]: upstream removed block pointers, so `tl.make_block_ptr` raises `NotImplementedError` and the kernels do not compile at all on any `main` tried — including `76940ad3`, the revision sglang pins for gfx1250 | **Unmeasured, expected neutral** — the same addressing expressed through masked plain pointers. The descriptor/TMA path was not enabled | Hand-written index arithmetic and masks replace `boundary_check` / `padding_option`. Feeding the remapped contextual/multi-target coordinates into pointer arithmetic is now a **silent layout bug** rather than a compile error. Perf against block pointers unmeasured | Revisit if upstream's `TensorDescriptor` gains an AMD lowering; `_should_enable_tma()` is `False` on HIP today because that path lowers to Hopper TMA. `scripts/attn_check.py` is the numeric guard if these kernels are touched again |
| 6 | **fbgemm host `kWarpSize` 64 → 32** — `include/fbgemm_gpu/utils/cuda_prelude.cuh`, patched in the Dockerfile | wave32: the host constant is a "stop-gap" 64 for ROCm while device compilation correctly uses 32, so the TBE backward launched `dim3(64, 32)` = 2048 threads and died on the 1024-per-block limit | **Unmeasured, structural.** Required for wave32 correctness; there is no wave64 configuration on this part to compare against | The wheel is then **single-arch**: wrong for wave64 CDNA parts. With it, TBE is bit-exact against CPU | Upstream fbgemm should derive the host constant from the device warp size instead of hardcoding per-vendor |
| 7 | **fbgemm codegen `items_per_warp` 256 → 128** — `codegen/genscript/jinja_environment.py`, patched in the Dockerfile | wave32: codegen emits `kThreadGroupSize` instantiations from `items_per_warp // 4` and hardcodes 256 for ROCm, which then disagrees with the patched call sites — undefined symbol at load | **Unmeasured, structural** — same reasoning as row 6 | Same single-arch caveat as row 6 | Same as row 6 |
| 8 | **`Fbgemm.cmake` fp16/bf16 convert sources added** — patched in the Dockerfile | At that fbgemm commit the file omits `FbgemmFloat16Convert*.cc` / `FbgemmBfloat16Convert*.cc` and relies on torch's bundled FBGEMM; amdprimus torch 2.11 exports no `fbgemm::Float16ToFloat_avx2`, so `fbgemm.so` fails to load | **None** — build-time source list only | Diverges from that commit's build and will conflict on an fbgemm bump | Upstream HEAD already ships this source list — drop the patch when bumping fbgemm |
| 9 | **`EMBEDDING_ROW_SCALE`** (runner default `0.25`) plus id modulo-wrap — `dlrm_v4/configs.py`, `dlrm_v4/datasets/yambda.py` | One ~432 GiB GPU against ~559 GiB of fp32 reference embedding tables | **Not a perf knob; it changes the workload.** Tables shrink to 140 GiB to fit one GPU, so throughput here is not comparable to a full-table run | **Changes the model**, not just its footprint: smaller vocabulary with ids wrapped into it. Not a valid MLPerf configuration, and accuracy is not comparable to the reference | Multi-GPU sharding to run unscaled; or a scale sweep to bound how much of the accuracy gap is the shrink |
| 10 | **Peak FLOPS keyed on `gcnArchName`** — `dlrm_v4/utils.py` | These parts report the generic name `AMD Radeon Graphics`, so `get_gpu_peak_flops` matched nothing and silently fell back to **MI350X's 2300 TFLOPS** — every earlier HFU number in this repo's logs is against the wrong chip | **None at runtime — it corrects reported numbers.** MFU/HFU only; earlier logs were scored against MI350X's 2300 TFLOPS | The historical denominator was 90 % of *silicon* peak; logs reported ~75 TFLOP/s and ~1 % HFU. The old global claim that gfx1250 has no matrix-core GEMM path is superseded by the current native WMMA evidence. Normalising against a measured kernel rate would conflate model efficiency with library/kernel selection | Revalidate the architecture/precision denominator and actual selected kernels on each stack; preserve the denominator used for each reported result |
| 11 | **`HSA_ENABLE_COREDUMP=0` + `HSA_COREDUMP_PATTERN=/tmp/gpucore.%p.gpu`** — runner defaults | A memory-access fault writes a coredump the size of allocated VRAM (9–36 GB observed), which otherwise lands in the working tree | **None** on the normal path; relocates fault dumps outside the working tree | `HSA_ENABLE_COREDUMP=0` is **not honoured** on this runtime, so the dump is relocated rather than suppressed — the disk write still happens | Runtime bug: honour the suppression knob |
| 12 | **Diagnostics** — `DEBUG_NAN_HOOKS` in `dlrm_v4/train/train_ranker.py`; `scripts/bisect_hooks/` (`sitecustomize.py` per-op Triton/PyTorch forcing, `gpu_cleanup.sh`) | Attributing [3]'s `nan` to the kernel that *manufactured* it, and attributing an e2e fault to a single op group — which is how [6] was placed on `hstu_preprocess_and_attention` | **None when off.** `DEBUG_NAN_HOOKS` and the bisect hooks add per-op synchronization when enabled | `DEBUG_NAN_HOOKS=2`'s pre-forward param sweep has itself wedged the GPU ([1b]), so it is opt-in, and hooks are unusable on 3.8.0 (they fault at `gstep=0`) | — |
| 13 | **`mul_u_activation_type` passed to the fused linear forward** — `ops/triton/triton_hstu_linear.py` | Inherited code bug, not a GPU bug: the forward dropped the parameter and applied no activation while its own backward applied one. One line takes that parameter grid from 32 mismatches to 0 | **Negligible** — one activation in the forward that the backward already applied | None | Send upstream. Dormant in the benchmark, which only ever passes `"none"` |
| 14 | **`&` / `\|` instead of Python `and` / `or` on masks** — `ops/triton/triton_position.py`. The other Triton-language change besides row 5; see [API changes](#63-triton-language-api-port-5) | Not compile-blocking today — the kernels already built — but Triton **warns and will error** on the Python operators applied to non-scalar tensors inside `@triton.jit`, so this is the second API accommodation for current upstream | **None** — the same predicate in a Triton-legal spelling | None. For boolean masks the two are equivalent: 3.6.0's `visit_BoolOp` already lowered `and`/`or` to `logical_and`/`logical_or` | None — and note the "this left loads unmasked" reading of it was **retracted**; see [Retracted](#10-retracted) |
| 15 | **`rows` widened to `int64` in `_ln_mul_dropout_fwd_rng`** — `rows_i64 = rows.to(tl.int64)` defined once at `ops/triton/triton_hstu_linear.py:186`, used by the loads (:189, :194), the mask-pointer reuse (:236) and **all eight `Y` stores** (:272-:310); no raw `rows[:, None]` survives in the kernel | **[3] int32 row-offset source bug, associated with the historical batch-1024 NaNs; the deterministic reproducer establishes this defect independently of later training failures.**<br>• **Mechanism:** the eight `Y` stores and the `X`/`U` loads indexed with a raw int32 `rows`, while the dropout-mask pointer three lines above *is* widened and every neighbouring kernel in the file widens too. At the production shape (`D = 4 heads x 128 = 512`, `concat_u=concat_x=True`) `stride_y = 3*D = 1536`, so `rows * stride_y` overflows int32 at **row 1,398,102** and the store lands at **`Y - 4.29 GB`**. `row_mask = rows < N` does not save it — the mask is computed on the *unwrapped* row id, so the wild store proceeds under a passing mask. **A bounds check and an address computed from different values** is the pattern worth recognising elsewhere in these kernels.<br>• **Why it is silent:** `Y` is a fresh `torch.empty` in a process already holding ~140 GiB of embedding table, so `Y - 4.29 GB` lands in *other live allocations*, not unmapped memory — no fault, just poisoned tensors, which is [3]'s exact recorded signature. It also explains the wandering onset (91 vs ~190 under an identical `SEED=1` stream): which tensor sits below `Y` is a per-process allocator accident.<br>• **Why it looks batch-gated but is not:** the autotune key is `key=["BLOCK_D"]`, so **codegen is identical at every batch size**. `N` is total jagged rows, so batch size enters only by scaling `N` past the wrap row. Under the in-use `interleaved` strategy (~2663 events/sample, an *inferred* figure from `yambda_5b.gin:463-478`, not the nominal 4086) N is 342 K at batch 128 and **2.74 M at 1024** — 48.9 % of rows wrapping, crossover at batch 523. Under `last_n` (~4085) it is 524 K / 4.19 M, crossover 342. **The verdict is the same either way**, so it does not rest on the one free parameter.<br>• **Verified on hardware, exactly:** `N=1398101` PASS; `N=1399126` FAIL with first bad row **1398102** — the predicted wrap row to the row — 1024 bad rows and **1,572,864 = 1024 x 1536 canary elements clobbered in an unrelated live tensor, zero faults**. `--fix` clean at every N.<br>• **Why only this kernel:** reached only when `use_separated_rng_ln_mul_dropout()` is true (`ops/utils.py:100-165`) — on AMD that means **gfx1250 + Triton >= 3.8**, itself the [4c] workaround. MI300/A100/H100 take the legacy `_ln_mul_dropout_fwd`, whose offsets are already widened. Arch-correlated without being an arch bug.<br>• **Not the batch-128 `nan` of 3.6.0/3.7.1** — that kernel is not on the path on those versions and batch 128 never approaches the wrap row ([9.4](history_8377ae9.md#94-the-batch-128-nan-on-360--371)).<br>• **Still an inference in one respect:** `N` is from corpus statistics in the gin file, not read out of the live kernel — the instrumented run wedged the node before printing.<br>• Handoff: [`lnmuldropout_i32_handoff.md`](lnmuldropout_i32_handoff.md). Reproducer: `AMDGCN_USE_BUFFER_OPS=0 python3 scripts/repro_gfx1250_lnmuldropout_i32.py --n 1399126 --canary-gib 2` (torch+triton only, deterministic with the documented canary allocation). Results: [3.1](#31-the-confirming-runs). | **Unmeasured, expected small.** One `int64` conversion hoisted to a single definition and reused by every load and store. No pre-fix batch-1024 baseline exists — it `nan`'d | **None.** A strict widening, matching what the dropout-mask pointer three lines below and every neighbouring kernel already did. `Mean + rows` / `Rstd + rows` are deliberately untouched (element stride 1, cannot reach 2**31 before `N` does).<br>**Backward kernels audited — no defect found:** `_ln_mul_dropout_bwd_dwdb` (:922) and the Helion variant (:2711) do use `rows[:, None] * D`, but their `N` is `tile_num = max(1, min(sms * 64, N // 4))` (~16 K), four orders of magnitude below the wrap row; the `dx_du` backwards already widen throughout (:523, :714-719, :2578-2583). **This audit found the row-offset defect in the separated forward; it was not an exclusion of other defects.** | Upstream the same widening to `mlcommons/training`. The legacy `_ln_mul_dropout_fwd` is already correct, so only the separated-RNG path (row 4) needs it. **Owner is this repo, not the LLVM/AMDGPU team** — the generated code faithfully implements what the source asks for, and the source asks for a 32-bit multiply. |

### Reversal criteria and measured costs

<a id="51-re-test-queue"></a><a id="53-cost-of-the-workarounds-still-in-place"></a>

The table above is the change record; this table is the review queue for a
future rollback. These are **dated prerequisites and measurements, not an
instruction to remove a gate now**. The original ranked queue and its full
September 11 evidence remain in [historical §5.1](history_8377ae9.md#51-re-test-queue).

| Change / defect | Evidence retained | What must be established before removal or replacement | Cost or benefit still to measure |
|---|---|---|---|
| Row 1, buffer ops off, [1]/[1b]/[2] | September 11 dispatch on `7ff97e3109` hung the node; compile-only showed mixed addressing above 2 GiB. [1b] also occurred with the knob off. The buffer-on position test pass was September 1 on **3.6.0**, not this pin. | Validate an updated backend against above-2-GiB mixed-addressing and matched knob-on/off controls; check both numerical correctness and hangs. Compile-only confirms codegen, not successful execution. | **Broadest unpriced workaround:** affects the pass group for every kernel. The historical A/B hung before producing a performance number. |
| Row 2, `BLOCK_N=64`, [6]/[4d] | At 8×8 heads, 13,809 tokens and D128: **11.22 ms versus 7.30 ms**, ±0.05 ms, **654 versus 987 VGPRs**, no spills in either measured kernel. | Validate the backend/rematerialization change with the standalone, varying jagged layouts, native A0/B0 controls, and training before unpinning. A later NaN pass does not establish this old fault is fixed. | **1.54× kernel slowdown** at the measured shape. No end-to-end recovery estimate: N128 faulted in training. |
| Row 3, `num_stages=1`, [4] | September 11: one unclamped batch1024 run completed **600 steps**, with `num_stages=3` verified. Older positive [4] evidence was batch8. | Port stale standalone block-pointer calls before retesting their mechanism; compare matched current kernels and longer training across repetitions. The historical bounded pass does not prove current NaN safety. | Unclamped **3491.99 ms/step** versus **3397.41 / 3403.39** clamped. Removing the clamp was about **2.8% slower** in this measurement; other shapes/stacks remain unpriced. |
| Row 4, separated RNG, [4c] | September 11 fused backward check passed with zero mismatches in dx/du/dweight/dbias/y and three masks. | Recheck the fused path on the intended compiler, then compare end-to-end numerics and performance. Review interaction with row 15: changing paths does not justify deleting its int64 fix. | Extra launches and memory traffic remain unmeasured; plausible fusion benefit is not a measured gain. |
| Row 5, plain-pointer port, [5] | 26 block-pointer and eight advance sites replaced; eight fixed BF16 cases passed on the two recorded stacks. | Keep compiling on supported Triton versions. If replacing it with a descriptor path, validate actual HIP lowering, physical address/mask separation, changing jagged layouts, and performance. | No block-pointer/plain-pointer performance A/B was recorded. |
| Rows 6–8, fbgemm build | Host warp and codegen changes support wave32; patched TBE matched CPU. The conversion-source patch resolved missing symbols. | Replace with upstream device-aware wave-size/codegen handling and conversion sources; validate on intended wave32/wave64 targets before deleting local patches. | Structural/build fixes; no valid wave64 baseline on this part. The patched wheel was architecture-specific. |
| Row 9, quarter vocabulary | Historical estimate **140.01 GiB versus 560.04 GiB**; IDs modulo-wrap. The current exact row accounting is in [full versus shrunk model](#full-versus-shrunk-model). | Use full tables with sharding for the reference model; validate placement, initialization, IDs and quality. A same-host scale comparison can measure shrink effects. | Vocabulary collisions and initialization change model behavior. Shrunk accuracy/throughput is not comparable to a benchmark-valid full-table run. |
| Rows 10–12, metrics/dumps/diagnostics | Architecture-aware peak fixes a reporting error; dump suppression was ignored; diagnostics synchronize only when enabled. | Keep correct metrics and useful opt-in diagnostics; revise dump handling when runtime suppression works. Validate disabled-path behavior before removing instrumentation. | No normal-path cost reported; diagnostic synchronization and large fault dumps have explicit operational cost. |
| Rows 13/15, source fixes; row 14, masks | Activation parameter grid **32→0 mismatches**; exact int32 wrap/canary reproduction; boolean masks equivalent. | **Retain source fixes.** Upstream or replace with equivalent correct implementations. Keep legal mask syntax as upstream evolves. | Source-fix overhead unmeasured/expected small; no justification for restoring incorrect indexing or dropped parameters. |
| Coverage and [7] follow-up | Only the selected attention autotune configuration had the recorded clean training runs; high-pressure concurrent-stream failures had a separate matrix. | Preserve the [15-config coverage requirement](history_8377ae9.md#51-re-test-queue) and [concurrency evidence](gfx1250_extvgpr_handoff.md). Do not infer a common mechanism from register pressure alone. | Coverage itself gives no measured speedup; alternate configurations may change both correctness and performance. |

The historical pipelining measurement is preserved in full:

| Batch1024, 600 steps | Step ms | Samples/s | Reported TFLOPS/GPU | Final loss | Window AUC |
|---|---|---|---|---|---|
| Unclamped, September 11 | 3491.99 | 293.2 | 50.0 | 0.13833 | 0.49938 |
| Clamped, run 1 | 3397.41 | 301.4 | 51.4 | 0.13826 | 0.49944 |
| Clamped, run 2 | 3403.39 | 300.9 | 51.3 | 0.13826 | 0.49940 |

These are historical measurements of that configuration, including its
vocabulary shrink and instrumentation. They do not price all workaround
costs, prove causation for the current NaNs, or establish competitive throughput.

### Historical reference and confirming runs

<a id="31-the-confirming-runs"></a><a id="12-against-the-mi350-reference"></a>

The MI350 RCP logs contain **60 runs**, 20 seeds at each of global batch8192,
16384 and 32768, all reaching AUC0.75. The September 10 A0 runs establish
bounded bring-up at local/global batch1024 with quarter tables. They do not
establish convergence or reference-model equivalence.

| Measure | MI350 RCP reference | Historical single-GPU MI450 A0 |
|---|---|---|
| Global batch | 8192 / 16384 / 32768 | 1024 |
| Samples | 69M to target at 8192; 112M at 32768 | 614,400 per 600-step run |
| Eval AUC | 0.7636–0.7875; 60/60 at least 0.75 | 0.4994353 / 0.4994041 |
| Seeds | 20 per batch | One seed, two separate runs |
| Embedding model | Full reference vocabulary | Scale0.25 and modulo IDs; not MLPerf-valid |

| September 10 batch1024 result | `b1024_fixed` | `b1024_fixed_rep2` | Before int64 row fix |
|---|---|---|---|
| Completed steps / exit | 600/600, exit0 | 600/600, exit0 | NaN at91 / approximately190; process could still exit0 |
| Logged losses / nonfinite | 60 / zero | 60 / zero | Nonfinite present |
| Final loss / range | 0.13826 / 0.13744–0.14013 | 0.13826 / 0.13743–0.14014 | 0.1377–0.1398, then NaN |
| Wall clock | 34m19s | 34m14s | Approximately10 min to onset |
| Window / lifetime AUC | 0.4994353 / 0.4994354 | 0.4994041 / 0.4994046 | — |

The driver was unloaded/reloaded and the container recreated between the two
recorded runs. Both used seed1, production history4086/sequence4096, three
HSTU layers, and expandable segments. The old stack was Triton `7ff97e3109`
with LLVM `b010a18d`, Torch `2.11.0+rocm7.14.0a20260625`, and fbgemm
`10b7757` plus rows6–8. Exact commands and settings remain in
[historical §2](history_8377ae9.md#2-the-working-stack).

The deterministic int32 reproducer passes at N1,398,101 and fails at
N1,399,126 starting at exactly row1,398,102, with **1,572,864 canary elements
overwritten**; widening fixes that test. The historical estimate N≈2.74M at
batch1024 came from corpus statistics, **not a captured live N in those
runs**. This distinction does not weaken the standalone source-defect proof;
it limits retrospective assignment of each training failure to that defect.
See [the retained int32 handoff](lnmuldropout_i32_handoff.md).

Secondary historical results also remain available: two batch128 runs of
4,000 steps (loss0.15350/0.15343), and the retired batch8 run of4,000 steps
(loss0.10000 versus the PyTorch path's0.1015). Those are separate bounded
observations in [historical §3.2](history_8377ae9.md#32-secondary-configurations).
The newer attempt405 and controlled replay evidence supersede the old claim
that only run length remained before calling the stack clean.

### Plain-pointer API port and numerical guard

<a id="63-triton-language-api-port-5"></a>

Upstream removed `tl.make_block_ptr` and `tl.advance`. The implemented port
uses **masked plain-pointer loads/stores**; `_should_enable_tma()` remained
false on HIP. It did not enable Hopper TMA or an AMD descriptor lowering.
The mapping below is retained as a review aid; address products must use a
wide enough integer type, as row15 demonstrates.

| Before (Triton ≤ 3.8 AMD SDK) | After (this branch; compiles on `main`) |
|---|---|
| `tl.make_block_ptr(base=X, shape=(N, D), strides=(stride_x, 1), offsets=(start_row, 0), block_shape=(BLOCK_N, BLOCK_D), order=(1, 0))` | `rows = start_row + tl.arange(0, BLOCK_N)` and `cols = tl.arange(0, BLOCK_D)` |
| `tl.load(ptr, boundary_check=(0, 1), padding_option="zero")` | `tl.load(X + rows[:, None] * stride_x + cols[None, :], mask=(rows < N)[:, None] & (cols < D)[None, :], other=0.0)` |
| `tl.store(ptr, y, boundary_check=(0, 1))` | `tl.store(Y + rows[:, None] * stride_y + cols[None, :], y, mask=…)` |
| `ptr = tl.advance(ptr, (0, BLOCK_N))` (attention K/V tiles) | rebuild the pointer from the physical sequence offset each tile: `K + offset_kh + (seq_start + load_offs_n)[:, None] * stride_kn + …` — there is no longer a cursor to advance |


The port covered **26 `make_block_ptr` and eight `advance` call sites** in
`ops/triton/triton_hstu_attention.py`, `triton_layer_norm.py`, and
`triton_hstu_linear.py`. Jagged concat/split, position and addmm already used
plain pointers or an unused descriptor path. Physical sequence offsets belong
in K/V addresses; remapped `pos_offs_*` belong only in causal/contextual/
multi-target masks. Mixing them silently changes the layout.

Eight deterministic BF16 cases passed forward and backward at `atol=1e-5`,
`rtol=0.016` on `7ff97e3109` and AMD `3.8.0+git4cff872c`, using
[`scripts/attn_check.py`](../../scripts/attn_check.py). These fixed-shape,
single-shot checks passed even while the varying-layout attention-backward
fault remained. Preserve both numerical checks and repeated layout coverage
when replacing this port. Full compatibility notes and historical invocation
are in [historical §6.3](history_8377ae9.md#63-triton-language-api-port-5).

### Retractions and retained historical evidence

<a id="10-retracted"></a><a id="11-change-inventory"></a>

These corrections travel with the restored history:

- Boolean `and`/`or` already lowered to logical operations for masks; changing
  them to `&`/`|` did **not** repair unmasked loads. The original attribution
  was retracted.
- Spilling alone did not explain historical attention defect[6]. Its passing
  high-scratch control and extended-register/rematerialization experiments
  remain in [historical §6.2](history_8377ae9.md#62-root-cause-of-the-attention-backward-fault-6--4d).
  They do not identify the current DW defect.
- One 4,000-iteration pass with `s_nop 7` after every WMMA was **not a fix**;
  an identical repeat faulted at0. That claim was retracted.
- Dropping `mul_u_activation_type` was an inherited source bug. Both that fix
  and the int64 row fix deserve preservation; the old “only source fix”
  description was inaccurate.
- Quarter-table modulo wrapping changes the model. The old assertion that
  none of the changes affected benchmark math was inaccurate.
- The old global assertion that gfx1250 lacked a matrix-core GEMM path is
  superseded by current native DW/WMMA evidence. Old HFU and throughput
  measurements remain dated measurements, not current kernel attribution.

The [complete `8377ae9` edition](history_8377ae9.md) retains every original
section, including the exact change table and ranked re-test queue. Its
statements about “current” state describe September1–11, not this session.
Use these links for the supporting record:

| Historical material | Retained location |
|---|---|
| Driver/image/fbgemm/dataset and performance context | [§8](history_8377ae9.md#8-the-node-and-environment) |
| Defect matrices, mitigation and reproducer commands | [§6](history_8377ae9.md#6-defect-and-port-reference-for-the-current-stack), [§7](history_8377ae9.md#7-reproducers) |
| Triton3.6/3.7/3.8 images, scheduler controls, batch128 and batch8 failures | [§9](history_8377ae9.md#9-historical-earlier-triton-versions) |
| File-by-file changes, Docker patches, gates, harnesses and diagnostic tools | [§11](history_8377ae9.md#11-change-inventory) |
| Extended-register and concurrency handoff | [gfx1250_extvgpr_handoff.md](gfx1250_extvgpr_handoff.md) |
| Int32 indexing proof, canaries and widening | [lnmuldropout_i32_handoff.md](lnmuldropout_i32_handoff.md) |

Older section bookmarks remain attached to this index of the preserved
historical edition:

| Original section | Historical record |
|---|---|
| <a id="1-status"></a><a id="11-where-this-stands"></a><a id="13-what-it-does-not-establish"></a>Historical bring-up status and limits | [Open section](history_8377ae9.md#1-status) |
| <a id="2-the-working-stack"></a><a id="22-run-configuration"></a><a id="23-build-and-run"></a><a id="24-operating-notes"></a>Historical stack, configuration, build and operating notes | [Open section](history_8377ae9.md#2-the-working-stack) |
| <a id="3-results-at-batch-1024"></a><a id="32-secondary-configurations"></a><a id="33-what-breaks-it"></a>Historical production/secondary results and failing controls | [Open section](history_8377ae9.md#3-results-at-batch-1024) |
| <a id="5-open-items"></a><a id="52-defect-detail"></a>Historical open items and detailed defect states | [Open section](history_8377ae9.md#5-open-items) |
| <a id="6-defect-and-port-reference-for-the-current-stack"></a><a id="61-defect-record"></a><a id="62-root-cause-of-the-attention-backward-fault-6--4d"></a>Historical defect record and attention isolation | [Open section](history_8377ae9.md#6-defect-and-port-reference-for-the-current-stack) |
| <a id="7-reproducers"></a>Historical reproducer inventory | [Open section](history_8377ae9.md#7-reproducers) |
| <a id="8-the-node-and-environment"></a><a id="82-image-fbgemm-and-dataset"></a><a id="83-performance"></a>Historical node, image, dataset and performance | [Open section](history_8377ae9.md#8-the-node-and-environment) |
| <a id="9-historical-earlier-triton-versions"></a><a id="91-images-tested"></a><a id="92-defects-by-version"></a>Earlier Triton images and defect matrix | [Open section](history_8377ae9.md#9-historical-earlier-triton-versions) |
| <a id="93-row-7-follow-up-coexec-scheduler-root-cause"></a>Coexec/expert scheduling controls | [Open section](history_8377ae9.md#93-row-7-follow-up-coexec-scheduler-root-cause) |
| <a id="94-the-batch-128-nan-on-360--371"></a>Earlier batch-128 NaN record | [Open section](history_8377ae9.md#94-the-batch-128-nan-on-360--371) |
| <a id="95-the-batch-8-pipelining-nan-4-pre-fix-stack"></a>Retired batch-8 pipelining record | [Open section](history_8377ae9.md#95-the-batch-8-pipelining-nan-4-pre-fix-stack) |

## Evidence and session recovery

<a id="28-capture-backup-and-retention"></a>
The [compact evidence manifest](evidence/current_20260919/manifest.json) records
the retained primary certificates and exact external source paths.
The porting/rollback tables and complete `8377ae9` edition are retained in
this directory. Full tensors, kernels, source archives, failed attempts and
additional investigation ledgers remain outside Git. Path abbreviations used above:

- **L:** `/home/chcai/mi450_logs/root_cause_20260919` — frozen launchers,
  certificates, logs, CPU audits, native decoding and resume helpers.
- **D, host:** `/home/chcai/dlrm_data/root_cause_20260919`; **D, container:**
  `/data/mlperf_dlrm_v4/root_cause_20260919` — captures, contexts, reports,
  private catalogs and native binaries.
- **Archived docs:** `/home/chcai/mi450_logs/a0_docs_archive_20260919_session_wrap_v1`,
  preserving the original `recommendation/docs` relative tree and hash manifest.
  This includes the old A0 main, detailed root-cause/value/attention ledgers,
  duplicate evidence and stale A0/B0 reconciliation.

The completed run's directory under D contains `context.json`, `sources.tar.gz`,
`outcome.json` and `boundaries/events.jsonl`; the native trace is its sibling
`<run>_hip.jsonl`, and the launcher/log are under L. CPU audit reports retain
whole-payload hashes. The smoke frame SHA256 is
`ee6afbdfd18049cd05381983d9f6e310f4bb94933a1a57147ce6a1197bc18855`;
the attempt-405 frame SHA256 is
`fd1b6ec93848368a519de2d5f01962cb9cf52688ca2ffbdcf6324ebcc7991a9e`.

The session handoff is `L/session_wrap_handoff_root_v1.md`. Historic Codex
context includes `/home/chcai/.codex/sessions/2026/09/18/rollout-2026-09-18T03-18-02-01a0b285-665f-7891-a208-59653434fc10.jsonl`; the retained evidence and source hashes, rather than a
session narrative, determine the numerical claims. The old session-wrap
handoff's running-state description is superseded by this completed-run audit.
The new session handoff is `L/session_resume_handoff_root_v2.md`.

All seven resume DW arms are terminal and their workers reaped. The final
KFD owner check is idle, the temporary CPU-only audit container is stopped,
and the original GPU container remained available at that snapshot. That
runtime snapshot is `L/session_resume_final_runtime_v1.json`. The investigation
subsequently resumed; current continuation results are recorded above.

Standing workspace rule: **no Git commit or push without an explicit user
instruction** (`/home/chcai/AGENTS.md`). This resume made local edits only.
The same file now records the user's rule to **keep actively investigating
until told to stop or a concrete blocker prevents progress**, rather than
ending after a bounded experiment or handoff.
The user explicitly invoked that stop condition on September 21. The latest
shutdown record is `L/session_20260921_restore_root_v1/session_stop_wrap.json`,
also [retained here](evidence/current_20260919/session_stop_20260921.json).
The superseding entry in `L/session_persist_progress_root_v1.md` replaces
the stale checkpoint that described candidate worker 61948 as running.
Earlier handoff instructions to continue or launch prepared work are historical
and do not override this stop.
